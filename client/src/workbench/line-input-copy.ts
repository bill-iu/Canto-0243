/**
 * Locked workbench line-input copy (label + placeholder).
 * Do not change without an explicit product decision — guarded by workbench-ui-self-check.
 */
export const WORKBENCH_LINE_INPUT_COPY = '歌詞/0243 碼/漢字加數字混合' as const;
