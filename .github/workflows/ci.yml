name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip
          cache-dependency-path: requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt
        env:
          PYTHONUTF8: "1"

      - name: Bootstrap CI lexicon data
        run: cp data/rime/fixtures/char_sample.csv data/rime/char.csv

      - name: Smoke tests
        run: python -m unittest discover -s tests/smoke -q
        env:
          PYTHONUTF8: "1"

      - name: Architecture seam checks
        run: python scripts/check_seams.py -q
        env:
          PYTHONUTF8: "1"

      - name: Guide example regression (Python)
        run: python -m unittest tests.test_guide_example_queries -q
        env:
          PYTHONUTF8: "1"
          DATABASE_URL: sqlite:///./tests/fixtures/lyrics.db
          READINESS_GATE_ENFORCE: "0"

      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: client/package-lock.json

      - name: Install PWA dependencies
        run: npm ci
        working-directory: client

      - name: Build portable host (/app)
        run: npm run build:portable
        working-directory: client

      - name: Portable host build self-check
        run: node scripts/portable-host-build-self-check.mjs
        working-directory: client

      - name: Guide probe readiness (PWA)
        run: npx tsx scripts/guide-probe-readiness-self-check.ts
        working-directory: client

      - name: Jyutping lookup parity (PWA PR#1)
        run: npx tsx scripts/jyutping-lookup-self-check.ts
        working-directory: client

      - name: Guide example regression (PWA)
        run: npx tsx scripts/guide-examples-self-check.ts ../tests/fixtures/lyrics.db
        working-directory: client
