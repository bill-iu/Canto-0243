"""Database layer: connection, bootstrap, dialect.

Canonical imports for new code::

    from app.db.connection import SessionLocal, engine, Base
    from app.db.bootstrap import bootstrap_local_db
    from app.db.dialect import contains_substring

``app.database`` remains a backward-compatible facade.
"""
