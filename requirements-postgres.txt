# requirements-postgres.txt
# 凍結中的 PostgreSQL scaffold（非產品承諾路徑；無整合測試）。
#
# 一般離線使用者只需：
#   pip install -r requirements.txt
#
# 若仍要實驗性使用 PostgreSQL：
#   pip install -r requirements-postgres.txt
#   # 設定 DATABASE_URL=postgresql://... 後啟動；啟動時會顯示凍結警告
#   alembic upgrade head   # 對齊既有 migration（新 schema 變更僅保證 SQLite）

psycopg2-binary
alembic
