# Product / Desktop runtime (ADR-0068 lean deps grill).
# Offline search path: SQLite + FastAPI. No on-the-fly Jyutping inject via pycantonese.
#
# Maintainer ingest / phoneme repair / ML:
#   pip install -r requirements-dev.txt
#   (or: pip install -e ".[ingest]")

fastapi
uvicorn
sqlalchemy
pydantic
python-multipart
python-dotenv
opencc-python-reimplemented  # trad_chinese s2hk (relation normalize; simplified input)
