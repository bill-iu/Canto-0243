# 單 digit 純碼搜尋只認最佳 pron_rank

純數字查詢且長度為 1 時，結果只收錄「該字面在詞庫全部單字讀音中 **pron_rank 最佳**」且碼命中（含當前檔鬆檔 variants）的列；命中多條時用既有權威讀音序揀一條展示。多位純碼（`22` 等）不套此閘，靠 essay／curated 排序沉底。罕見若為該字唯一／最佳讀仍可出。目的：避免多讀收錄後單碼結果被罕見／錯誤讀音單字淹沒。
