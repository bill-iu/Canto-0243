# Canto-0243

離線粵語填詞查韻工具：依 0243／02493 數字碼、粵拼與韻母規則，幫創作者找出可替換的詞條與近義／反義關係。

## 如何使用本文件

本文件是**技術領域詞彙表**，不是實作規格。討論功能、測試或領域行為時，優先使用下列正名；`_Avoid_` 列出易混淆或禁用的說法。

**收錄邊界**：只記**領域概念與技術行為**（詞條、查詢分派、就緒閘職責等）。**不記**視覺、互動、文案語氣、品牌或任何藝術／設計方案——開源允許 fork 採用完全不同的設計概念，此類決策不應綁死在共用詞彙表。

| 章節 | 內容 |
|------|------|
| 核心資料 | 詞條、字面、0243 碼、粵拼、聲母／韻母 |
| 詞性與分類 | 專案自建詞性、詞類、語彙族、語態面 |
| 比對關係 | 同韻、韻母比對檔、正韻、通韻、腹韻、尾韻、韻母分組表、整詞放寬韻索引併查、韻母比對檔排序、同聲、同音、工作台碼約束檔、工作台選定讀音、字位鎖定、清鎖定、替換段、工作台起句、句稿復原、通配符格、韻／聲參考字串、清空句格 |
| 搜尋模式 | 搜尋模式家族、0243搜尋、近反義、近反義池、靜態詞林埠 |
| 查詢語法 | 基礎規則、分派優先序、各語法家族 |
| 詞庫與排序 | 詞庫埠、收錄決策、參考字讀音解析、詞條從源重建、詞條庫建置流程、戰役工具、音節拼接讀音、essay 詞頻、排序用 curated、結果誰靠前、擷取頁／結果數／呈現批次 |
| 產品邊界 | 離線交付、創作者、分渠道發佈、PWA、就緒閘、搜尋教學、搜尋指南、押韻指南、搜尋教學速覽、套件發佈指紋／套件更新提示 |

---

## 核心資料

**詞條**：
字典中一條可查記錄，由字面、0243 碼與粵拼組成；同一字面可有多條詞條（多讀音）。**詞條庫**內 canonical 列以 **(字面, 粵拼)** 唯一；邊個 upstream 貢獻由 **詞條來源紀錄** 追蹤。
_Avoid_：Word（實作名）、候選（僅在描述搜尋結果清單時使用）、用 0243 碼單獨當 canonical 唯一鍵（碼由粵拼衍生）

**詞條來源紀錄**：
**詞條從源重建** 時記錄 canonical **詞條** 與 **詞條 SSOT 來源** 之間嘅 provenance；一條 **詞條** 可對應多個來源行（多源同意同一讀音）。唔 duplicate **詞條** 上已有嘅字面／粵拼／0243 碼。
_Avoid_：provenance（作領域正名）、把來源紀錄當第二套讀音倉庫、在來源紀錄重複存 jyutping

**Unihan 粵音來源**：
Unicode Consortium 發佈、按 Unicode 版本固定嘅逐字粵音候選；匯入粵音相關欄位時保留來源版本、碼位同原 property（例如 `kCantonese`、`kCheungBauer`），作已收錄漢字嘅其中一個加法讀音來源。佢唔係**收錄決策**入口：只豐富已由其他可信來源收錄、或同批已確定新增嘅字面，唔因 Unihan 有碼位／讀音就自行開新字入庫。亦唔係詞語層級讀音權威，唔無條件覆蓋其他可靠來源；欄位互相或與詞語讀音衝突時入稽核。例：`𡁻` 因 `牛𡁻牡丹` 確定新增而可引用 Unihan；其 `ziu6` 由 `kCheungBauer` 支持，`zeu6` 另由**粵音資料集叢**支持，唔將 `kCantonese` 嘅 `ziu1` 靜默改寫成 `ziu6`。
_Avoid_：只記「Unihan」而遺失 property／版本、把 Unihan 當全量漢字收錄門、把逐字讀音拼成所有詞語嘅唯一標音、以 provisional `kCantonese` 蓋過詞語級證據、把第三方彙編頁誤稱為 Unihan 原始欄位

**完整漢字字集**：
詞庫匯入、後端查詢同前端輸入共用嘅有效漢字邊界，按完整 Unicode `Script=Han` 判定，涵蓋基本區及 supplementary-plane 擴展區；保留來源原字，唔為通過舊 BMP-only regex 而代換異體／近義字。例：`𡁻`（U+2107B）合法，`牛𡁻牡丹` 唔改成 `牛嚼牡丹`。
_Avoid_：只接受 `\u3400-\u9fff`、為單一碼位加 allowlist、以字形相近字靜默正規化、前後端各自維護不一致範圍

**字面**：
詞條上顯示的漢字序列（一至多字），例如「香港」的字面長度為 2。
_Avoid_：詞面、字串

**程式識別子（canto 字）**：
討論或撰寫程式時，指 CJK 字元嘅識別子分工：**`chars`** 指字元序列／字串操作／regex 範圍（如 `mask_chars`、`q_chars`）；**`canto`** 指單一粵語書面字、音節槽內一格（如 `canto_char`、`has_canto`）。領域討論、UI 文案同 hint 仍用**漢字**／**字面**；`canto`／`chars` 只限程式命名。歷史識別子 **`literal_char`**（MatchSpec slot kind）語意等同固定 canto 槽，保留唔改；新 code 唔再新增 `*_char` 識別子。
_Avoid_：`hanzi`、`Hanzi`、`_HANZI_*`、把 `chars` 同 `canto` 混用於同一概念、喺用戶文案改用 `canto`/`chars`、新 code 用 `*_char` 命名（`literal_char` 除外）

**394052 碼**：
附在詞條上的聲調數字序列；數字集為 `3`、`9`、`4`、`0`、`5`、`2`（模式名即字母表）。逐音節由粵拼調值映射：`1→3`、`2→9`、`3→4`、`4→0`、`5→5`、`6→2`；**三聲與五聲分別為 `4` 與 `5`**（唔再合併）。長度與字面音節數一致。詞庫存儲一律用此規範形。
_Avoid_：code（實作名）、音碼、把 394052 碼當韻母鍵盤映射

**0243 碼**（舊）：
**394052 碼** 落地前嘅歷史存儲形；數字集 `0`／`2`／`3`／`4`／`9`，粵拼三聲與五聲皆映射為 `4`。遷移採**收錄管線一次重建**；新版詞庫**唔**雙讀舊形，舊碼只作歷史說明或外部對照。
_Avoid_：把 0243 碼當現行詞庫格式、九鍵（易與電話鍵盤混淆）、runtime 同時認舊存儲 `4` 為五聲

**02493 碼**：
查詢輸入可含 **394052 碼**以外嘅擴展數字（`1`、`6`、`7`、`8`），以輸入層分清細分；比對前須逐位正規化為 **394052 碼**再對詞庫。擴展映射（與舊做法相同）：`1→3`、`6→2`、`7→3`、`8→4`（`1` 與 `7` 皆落入三聲類 `3`）；**394052 字母表內**（`0`、`2`、`3`、`4`、`5`、`9`）恆等，其中 **`5→5`**（五聲，唔再併入 `4`）。`1`／`7`／`8` **本身唔做鬆檔對**；鬆檔只作用於正規化後嘅 **394052 位**（見 **0243模式**／**02493模式**）。**0243搜尋模式**內凡作聲調數字出現嘅位皆適用。**0243模式**與 **02493模式**共用上述正規化；鬆檔差異見兩模式定義（**02493模式**僅 **`4↔5`**）。**0243搜尋模式**內零結果：**無其他 hint** 時只顯示粗體「搵唔到「{查詢}」。」（唔加第二行）；**已有其他 hint** 時第一行「搵唔到」、第二行保留原 hint。
_Avoid_：9鍵、九鍵、9-key（實為 9 **碼**／數碼，唔係電話九宮格）、把 02493 碼當詞庫存儲格式、查詢 `5` 仍映射為 `4`（已廢棄）

**粵拼**：
詞條的粵語羅馬字標注，一音節一段（如「香港」→ `hoeng1 gong2`），與 **394052 碼**逐音節對應。
_Avoid_：jyutping、拼音（易與普通話混淆）、拉丁、拉丁字母、字母（作產品或教學文案時）

**讀音**：
詞條上完整音節的粵拼（如「你」→ `nei5`）。查詢比對時，參考字多半只提供**聲母或韻母其中一維**作錨；0243 碼數字則單獨約束該格聲調，與參考字是 `nei5` 還是 `nei1` 無關。
_Avoid_：把參考字的整段讀音等同於該格的查詢條件

**聲母**：
粵拼音節的輔音部分（如 `gong2` 的 `g`）；同聲比對以聲母為單位。
_Avoid_：initial、字頭

**韻母**：
粵拼音節的韻部（含調，如 `gong2` 的 `ong2`）；同韻比對以韻母為單位，與 0243 碼的聲調維度不同。
**y- 韻核**：辅音（叢集）後接 `y` 再接元音時（如 `jyu`、`zyu`、`syu`），`y` 與後續元音屬**韻母**（如 `yu`、`yut`），唔併入聲母叢集（如唔把 `zyu` 拆成聲母 `zy`、韻母 `u`）。
**u／yu 韻部**：`u` 與 `yu` 為不同韻母鍵（如 `fu` 與 `zyu`／`住`）；同韻比對**唔**跨系匹配。
**獨立鼻音韻母**：粵拼**整個音節**僅為 `m` 或 `ng`（加調，如唔 `m4`、吾 `m4`、五 `ng5`、午 `ng5`）；該字面**無聲母**，查詢比對上**只**屬韻母維度；`m` 與 `ng` **完全等價**（mrpinyin M／NG 欄）。唔同「門」`mun` 嘅輔音聲母 `m`，亦唔同「我」`ngo` 嘅聲母叢集 `ng`。
_Avoid_：final、字尾、把唔／五／午等獨立鼻音音節當有聲母、把 codec 存入 `initials` 嘅 `m`／`ng` 當聲母錨匹配、把 `zyu` 韻部當 `u`、用韻母粵拼錨 `u` 匹配 `yu` 音節

---

## 詞性與分類

**專案自建詞性**：
維護者維護、一源多消嘅詞條分類體系；三軸正交：**詞類**、**語彙族**、**語態面**。權威係 **專案自建詞性清單**；runtime 以**獨立詞性載體**載入（**唔**經 **詞條庫** 主檔欄位套入）。消費者包括：近反義 campaign「同詞性」品質閘、**詞條**面板展示、搜尋結果與工作台之 **三軸詞性篩選**（創作者主動開先收窄；**唔**按起句子面種子自動同桶濾候選）。**掛載單位**為**字面**（唔按 (字面, 粵拼) 分列）；同一字面各讀音詞條共用同一套三軸標籤。唔等於上游詞典 POS、唔等於 COW 實驗四桶 alone。
_Avoid_：POS（作唯一領域正名）、把三軸打成一個扁平板枚舉、把成語當與名詞互斥嘅「詞性」、三份各自 map、第一期做讀音級 override、把詞性權威寫死喺 lyrics.db 主表而廢清單、工作台預設按種子閘用詞類靜默剔候選

**詞類**：
**專案自建詞性** 第一軸閉集：**名**、**動**、**形**、**副**、**虛**（五主類）與 **未定**。可多標（如「好」→ 形+動）。**虛**收介、連、助、語氣、代、量、數、嘆等封閉類，第一期唔再拆。**未定**＝待審。同桶／同詞性只計 **閘用詞類**（見 **詞性信任**），唔係清單上每一個五主類碼都入閘。
_Avoid_：把成語／俗語／諺語當詞類值、把語態當詞類、強迫一字面只得一個詞類、第一期拆及物／專名子類、用未定做硬拒、把 COW 單標當已審閘用詞類

**語彙族**：
**專案自建詞性** 第二軸：固定表達分類；與 **詞類** 正交。權威閉集（一字面至多一值；普通詞＝空，唔另開「普通」）：**熟語**（傘）、**成語**、**俗語**、**諺語**、**歇後語**。四葉**蘊涵**熟語（語意上葉⊂熟語，存儲仍單值、唔雙寫傘+葉）。單獨 **熟語**＝確認固定語、葉未審或跨界待決。標註優先序：（1）有高信任結構化 **歇後語** `term` 身份→**歇後語**；（2）其餘先判 **成語**（定型＋典故／對仗／高度非組合義至少一項；唔單靠字面長 4）；（3）其餘口語定型→**俗語**（口語優先於「似諺語」句式）；（4）其餘書面固定語→**諺語**；（5）分唔清→留 **熟語**。長度 4 之普通複合詞留空，**唔**因長度自動入族。教用可稱「四字詞」，領域正名仍係 **熟語**／其葉。**同詞性**硬閘**只睇詞類**，唔睇本軸。
_Avoid_：把語彙族當唯一 POS、把成語／俗語／諺語／歇後語當詞類、用字面長度 4 自動代理真成語、與 campaign len4 母體混稱已審成語、以「四字詞」作唯一正名而排除長諺語／俗語、普通詞另開枚舉、葉與傘雙寫兩欄、要求全庫葉級四選一先算可用、從外源自由正文猜語彙族、把外源固定語庫當 runtime 依賴或第二套詞庫

**歇後語**：
**語彙族**葉類：由引子（`term`）同後語（`answer`）構成嘅兩段式固定表達；分類掛喺引子字面。後語可注入**詞條庫**，但唔因擔任後語而自動取得 **歇後語** family；只有同時喺另一筆擔任引子先取得。高信任外源結構化 content type 可審定本葉，並優先於同字面既有較泛 family；舊值留審核紀錄。引子嘅**詞類**表示整個表達喺句中嘅功能：逐筆以後語整體句法功能為主要證據，再用釋義／例句驗證；可多詞類，證據衝突或只可判完整小句則留 **未定**。family 信任同詞類信任分開審核。
_Avoid_：把每個後語都標歇後語、把成對全文當單一 POS 掛載字面、從 explanation 關鍵詞猜本葉、抽一個後語核心語素機械四分詞類、強迫每筆單一詞類、為保留舊 family 把語彙族改成多值

**固定語來源快照**：
外部固定語來源（現行 Cantonese.md）由維護者明示刷新至指定 upstream commit；manifest 固定 commit SHA、archive SHA-256、content-type mapping 同解析／分類統計。一般**詞條從源重建**只重放固定快照，唔跟分支頭漂移；刷新先輸出新增／改類／刪除 diff，經審核先取代快照。**Unihan 粵音來源**同樣固定 Unicode release version。
_Avoid_：建庫時直接抓 `main`／latest、只有 URL 無內容 hash、上游改類或刪除後靜默套用、不同 Unicode release 混用而無版本記錄

**三軸詞性篩選**：
創作者喺搜尋結果清單與句格工作台候選上，按 **詞類**／**語彙族**／**語態面**主動收窄。軸內多選係 OR，非空軸之間係 AND；未選之軸唔限制。**名詞**／**動詞**／**形容詞**／**副詞**／**虛詞**按高信任展示詞類交集匹配；**成語**／**俗語**／**諺語**／**歇後語**各只匹配該葉，**熟語**匹配全族（傘∪四葉）；**主動**／**被動**精確匹配。字面缺任何已啓用軸之高信任展示標籤時**唔入選**。葉篩**唔**把未細分之 **熟語**傘列算入該葉。schema／載體支援葉值即提供本篩（唔等葉覆蓋達標）；初期葉結果偏少屬預期。**唔**改 campaign **同詞性**閘用詞類契約；**唔**等於工作台按種子自動同桶。
_Avoid_：軸與軸用 OR、葉篩吞併傘列、缺已啓用軸標籤當通過、用中信任閘用詞類冒充創作者展示篩選、未達覆蓋就隱藏篩選控件、用本篩代替 campaign 同詞性、未開本篩仍按起句種子剔候選

**語態面**：
**專案自建詞性** 第三軸：取值 **主動**／**被動** 或空。預設全空；**被動** 標固定被動／遭受義（**含被動義名詞**，如受益人／獲益），或構詞字首 ∈ **被／捱／受／遭／讓／給／叫／獲／挨／得**（≥2 字；啓發式提案；正式清單經**詞性提案審核**；「得」多見於得救一類，唔等於凡「得」皆被動）。**主動** 主要喺同被動條成對對照時先標，唔為普通動詞自動標主動。其他來源（如 COW）標詞類時，若命中上述構詞字首，**保留詞類、併被動語態**（唔因來源蓋過語態）。屬字面級標籤，唔做句級語態分析。campaign 同詞性**第一期唔以本軸做硬過濾**；面板有標先顯示（高信任）。
_Avoid_：voice（作領域正名）、全庫動詞必標主動／被動、把語態併入詞類枚舉、句級「被」字句分析當詞性、COW／草稿來源蓋掉已命中之被動構詞語態、啓發式未審就當正式清單

**詞性信任**：
清單列嘅可信度階梯，決定邊啲標籤可當 **閘用詞類**／創作者可見。**高**：人手、`review` 過審、啓發式（含 seed）。**中**：COW 多標（`cow-multi`）——可入 campaign 硬閘，**唔**作創作者主展示。**低**：COW 單標（`cow-single`）、fallback／`u` 起草——**只** provenance／日後重審，**當缺標**（唔硬拒、面板唔展示詞類 chips）。COW 只可作 **詞性提案** 來源；runtime **唔**直讀 COW 檔。
_Avoid_：COW 當第二套權威、cow-single 入同詞性硬閘、把清單有碼等同已審

**閘用詞類**：
字面用於 campaign「同詞性」硬閘嘅 **詞類** 集合＝**詞性信任** 為高或中、且屬五主類（非僅 **未定**）之碼。低信任或空→本集合為空（**詞性缺標**）。
_Avoid_：用低信任 COW 單標做硬拒、把閘用詞類當工作台默認候選過濾

**詞性缺標**：
字面無 **閘用詞類**（空、僅 **未定**、或僅低信任起草）時：**缺標 ≠ 錯標**。campaign「同詞性」：任一方缺閘用詞類則**唔**以詞類硬拒；兩邊皆有閘用詞類且集合相交為空先可拒。**句格工作台**候選**唔**預設按起句種子做閘用詞類同桶過濾；創作者開 **三軸詞性篩選** 先收窄。**詞條**面板：只展示 **高** 信任之詞類／語彙族／語態；三軸皆無可展示則唔顯示詞性區；**未定** 與低信任預設唔對創作者展示。
_Avoid_：用空集合冒充已分類、缺標當異類硬拒、創作者 UI 常駐「未知／未定」嘈音、面板展示 cow-single 草稿、工作台無手動篩選仍按種子詞性靜默剔候選

**詞性覆蓋母體**：
**專案自建詞性** 可稽核完成範圍（v1）：**詞類** 母體＝近反義 campaign 頭尾字面 ∪ Essay 詞頻 Top-K ∪ 工作台／黃金查詢種子；母體內每字面有終局（正式五主類或顯式 **未定**；低信任起草亦算終局列）。**熟語** 傘另 freeze 候選子集（優先字面長 4；P2 傘品質 **>90%**）。**語彙族細分** 母體＝清單上已標 **熟語**（傘）之字面；漸進改葉，既有傘列保留至有過審提案；終局＝三葉之一 **或** 顯式留 **熟語**；品質抽樣 **>90%**。**語態面** 無覆蓋率 KPI。推進：P0 campaign → **P1 Essay Top-5000**（終局 + 閘用品質 **>90%**）→ **P2 熟語傘** → **語彙族細分**（葉）→ **P3 長尾**（Essay rank **5001–20000** 終局覆蓋；對 P3∩閘用 抽樣品質 **>90%** 兩輪確認）。v1 **唔**以全詞庫每字面皆有閘用詞類或皆有葉級語彙族為完成條件。
_Avoid_：宣稱全庫語言學完備、無閘自動寫入權威 SSOT、把缺母體外長尾當 v1 未完成、P1 用絕對詞頻門檻代替 Top-K、把終局覆蓋誤當閘用 90% 品質閘、單輪抽樣未過就宣稱品質閘已過、長度 4 自動當熟語、把「成語篩結果少」當細分未完成

**詞性提案**：
入 **專案自建詞性清單** 前嘅非權威草稿列（可來自人手、LLM、COW 四桶 seed、啓發式、上游 POS 特徵、**外部成語 membership** 等）。P0／P1 偏詞類；P2 熟語傘；其後 **成語** 葉可經外部成語字面集繁化後與 **詞庫** 字面交集產提案；語態用 **語態面** 構詞字首（被／捱／受／遭／讓／給／叫／獲／挨／得）等啓發式加對照補主動。上游 POS、COW、外部成語庫 **唔**直灌 SSOT；外部成語工具**只**服務離線提案，**唔**作本產品 dependency／runtime／第二詞庫。
_Avoid_：把提案當已發佈詞性、無審核寫入清單、把 china-idiom 等外庫併入發佈依賴或查詢路徑

**詞性提案審核**：
**詞性提案** → 權威清單嘅必經閘。預設由 **agent 審核**（含 subagent 批量覆核）根據 **詞類**／**語彙族**／**語態面** 契約挑錯、標 **未定** 或打回；維護者可覆核或否決。過審先 append／改 **專案自建詞性清單**。實作缺陷另開修正，唔喺領域上放寬「提案即清單」。
_Avoid_：跳過審核直入清單、用抽樣百分比代替本閘而不作提案級審核（除非維護者顯式另訂批級閘）

**同詞性**：
近反義品質語境下，兩邊字面之 **閘用詞類** 集合相交非空。任一方 **詞性缺標** 則本條件不適用（唔硬拒）。只看閘用 **詞類**，唔看 **語彙族**／**語態面**。P0 完成且 **`p0_hard_gate`** 開後，兩邊皆有閘用詞類且相交為空先硬拒。面板只展示高信任。**唔**等同工作台預設候選過濾。
_Avoid_：要求語彙族相同、要求語態相同、把未定或 cow-single 當閘用詞類、P0 未完成就用半套標籤硬殺合格對、把 campaign 同詞性套用做工作台默認篩選

**專案自建詞性清單**：
git 追蹤嘅已審定字面→三軸標籤表（維護者 SSOT）。一列一 **字面**；**詞類** 可多值；**語彙族**／**語態面** 可空。**只收 ∈ 詞庫字面集 嘅字面**（與 **詞條庫** 成員資格對齊）；Essay 切詞殘片、未入庫字串**唔**入清單。只收經 **詞性提案審核** 之列；**詞性提案** 另存。正式清單唔收 pending 幽靈字。創作者**只讀**；**唔**經關係補錄／詞庫勘誤介面改。難標但 ∈ 詞庫者可留 **未定** 或低信任，另開修復批。**詞性碎片** 之 residual 字面**唔**以獨立 formal 列留主表（見 **詞性字面別名**）。
_Avoid_：把 raw 生成當 SSOT、與反義／近義／勘誤／關係補錄混檔、創作者可寫詞性、把未入詞庫嘅 Essay 膠連／碎片當 POS 權威列、殘字主表 formal 毒閘

**詞性碎片**：
∈ 詞庫、但唔宜當獨立閘用詞位嘅字面，分三類（note 分號 token：`fragment;residual`｜`fragment;clause-slice`｜`fragment;opaque`）。**residual**：複合詞殘字（如「曱」／「甴」），POS 經 **詞性字面別名** 指向完整詞。**clause-slice**：截斷句／半語（如「我見」「個月」），主表可留 **未定**，永唔批量升 formal。**opaque**：庫內有、語義暫不清，留 **未定** 至人手改判。碎片類型用於 **詞性雙軌覆蓋** 分母，唔等於踢出詞庫。
_Avoid_：一律 formal 殘字、一律刪庫、把自由語素當 residual、用 substring 匹配 `fragment` 誤傷其他 note

**詞性字面別名**：
git 追蹤表（`data/pos/alias.tsv`）：`source`（殘字等）→ `target`（完整詞字面）。權威入帳**人手**（或人手確認後 apply）；CLI 可出高信心**提案**但**唔** silent merge。完整詞缺庫時先 **詞級標音 curated** 再入清單（可同 PR）。殘字暫**留**詞庫 membership。詳見 ADR-0060。
_Avoid_：POS 腳本旁路寫詞庫、寬鬆自動 alias 自由語素、主表與 alias 雙寫同一 residual formal 列

**詞性雙軌覆蓋**：
庫內難標修復之完成度報兩個率：`formal/all`（全清單）與 `formal/(all−fragment)`（扣已標 **詞性碎片**）。v1 戰役收工：後者 **≥95%** 且碎片皆有類型 token；**唔**要求全庫 `u=0`。大批 agent 晉升 formal 須**每批**抽樣品質閘 **>90%** 先合入。
_Avoid_：只報 formal/all 自稱完工、無碎片標籤就扣分母、跳過批級品質閘

**庫內難標 v1 收口**（2026-07）：
**專案自建詞性清單** 內，非 **詞性碎片** 字面已全有正式五主類（`formal/(all−fragment)=1`）；`formal/all≈0.999`。剩餘 **未定** 僅 **詞性碎片**（clause-slice／opaque，及待完整詞之 residual）。Essay 非碎片 still-u 佇列已空；五批 top-N 複核 ok_rate≈0.97。**詞性字面別名** 權威表 `data/pos/alias.tsv`（含 curated 完整詞如 曱甴／踊躍／魍魎）。新詞入庫後以維護模式小批標註，唔重開 v1 戰役。詳見 ADR-0060、`data/pos/audit/nf_batches_full_gate/`。

China-idiom 與完整詞庫相交但原先缺 POS 嘅 4,147 筆已另批過審並加入清單：208 筆有高信任正式詞類，3,939 筆因缺可靠句法證據明確留 **未定**，4 筆有固定 **被動**；語彙族皆為 **成語**，`deferred_missing_project_pos=0`。本批唔改上述 Essay／fragment 戰役分母與完成定義。
_Avoid_：把仍有碎片未定當 v1 未收口、為湊 u=0 而 formal 截斷句、重開全庫 top-N 而無新 membership

**詞性載體**：
由 **專案自建詞性清單** 發佈出、供 Portable／PWA／campaign 工具 runtime 載入嘅獨立資料（與 **詞條庫** 主檔分離）。可喺**唔**做 **詞庫版本 bump** 嘅情況下更新詞性內容；有自己嘅完整性／版本標識，令快取客戶端可淘汰舊載體。與詞庫版本**允許短期漂移**；查詢引擎須容忍載體未就緒或舊載體（回退 **詞性缺標** 行為，唔阻 **就緒閘解鎖**）。可選附帶 **詞性字面別名** 映射；缺則 source 當缺標。
_Avoid_：pos map（作領域正名）、強制詞性更新綁死 lyrics.db 同版、詞性載體缺失就卡住就緒閘、PWA／Portable 各維護漂移副本無版本

---

## 比對關係

**同韻**：
對應音節位置上開啓**韻母**維度約束（搜尋列 `=`／韻錨、句格勾同韻、粵拼韻字母錨等）；是否命中由當前 **韻母比對檔** 決定。預設檔下等價「整段韻母一致」。
_Avoid_：押韻（指比對規則時）、rhyme、把同韻等同永遠精準全韻母、把同韻當頂層搜尋模式

**韻母比對檔**：
與聲調精度、碼約束、同聲正交嘅閉集，只喺有 **同韻**／final 錨時**影響命中**：(**正韻**｜**通韻**｜**腹韻**｜**尾韻**)。預設 **正韻**。搜尋列與句格工作台共用同一檔；複合查詢末字韻若走 final 錨則跟同一檔。唔另開頂層搜尋模式。**創作者 UI 展示名**可為 **押韻模式**（控件 label）；領域／ADR／解釋 IR 正名仍係本詞。句格位罝：本次替換條件第一行 menus，順序 `聲調精度 | 押韻模式 | 原意關係 | 0243 碼`；option 文案正韻／通韻／腹韻／尾韻。控件**常駐可改**（搜尋殼＋句格條件區），語感同 0243 三檔：無韻錨時改檔**唔改當前結果**，但可預選畀下一次有同韻／`=` 嘅查詢用——**唔**因無 `=` 就 disabled（避免被迫先打 `=` 搜一次先改到檔）。狀態屬**工作階段／查詢分頁**：同 tab 內搜尋與句格同步；新 tab 或硬刷新回 **正韻**；**唔**寫入分享 URL；第一期**唔**做跨日 localStorage 永久記住。
_Avoid_：把「押韻模式」當頂層搜尋 mode 或 URL mode 枚舉、精準押韻（作檔名）、rhyme mode、同 0243／近反義並列嘅第四套搜尋家族、搜尋與句格各存各檔而行為分叉、把檔寫進可分享連結令同一查詢異結果、第一期永久記住導致「忘記自己開咗通韻」、無韻錨就 disabled 強迫先 `=` 搜一次、用「押韻模式」取代領域正名寫進規格／IR

**正韻**：
**韻母比對檔** 最嚴一檔：介音、母音長短、韻尾皆須吻合（整段粵拼韻母一致）。等價本工具歷來 **同韻** 行為。
_Avoid_：精準押韻、嚴格模式、精確韻母（作正名）

**通韻**：
**韻母比對檔** 一檔：多個粵拼韻母按 **韻母分組表** 之通韻**部**併組後再比。舒聲**部**命名參考粵曲二十轍系（rhyme2；產品用「部」唔用「轍」）；僅含一個細韻嘅組稱「韻」（如雞啼韻）。**舒入相配**：入聲歸入對應舒聲部，同部互通。
_Avoid_：傳統押韻（作唯一正名）、把通韻當頂層 mode、入聲另部只互通入聲當預設、產品文案用「轍」作通韻組正名

**腹韻**：
**韻母比對檔** 一檔：只要求韻腹／核心母音類相同，忽略韻尾（assonance／斜韻取向）；分組以 **韻母分組表** 為準。
_Avoid_：字腹韻、僅押元音、韻腹（作唯一正名）

**尾韻**：
**韻母比對檔** 一檔：只要求韻尾／coda（或開尾）類相同，忽略主要母音；分組以 **韻母分組表** 為準。**唔**等同語法教學裏「詞末位置嘅韻錨」（`字=`）；後者仍叫詞末韻錨／等號韻，唔佔本檔名。
_Avoid_：押尾音、韻尾（作唯一正名）、同「詞末 `字=`」混稱

**韻母分組表**：
維護者 git 追蹤、runtime 唯讀嘅靜態分組，定義 **通韻**／**腹韻**／**尾韻** 下邊啲粵拼韻母視為同類；可附教學用**部**／**韻**名。**通韻**舒聲按粵曲二十轍系結構組成「部」（多韻互通單位）或單細「韻」；+ **舒入相配**（入聲 `-p/-t/-k` 併入對應舒聲部，跟 rhyme.txt；教學唔另開入聲卡）。表外／未列韻母唔發明新部，比對回退該音節 **正韻**。改表＝資料變更，唔改比對檔閉集形狀。
_Avoid_：runtime 即興 cluster、無表靠字串前綴猜通韻、入聲策略同表分離成第二套隱藏規則、缺表項就丟錨或當全庫通配、為放寬整詞查詢喺 **詞條庫** 加分組 id 欄（體積；用 runtime 音位索引多 key 併查）、通韻入聲另部只互通入聲、產品通韻組正名用「轍」

**整詞放寬韻索引併查**（產品行為名；實作可稱 multi-key union／intersect）：
當 **韻母比對檔** 非 **正韻**、且查詢為**整詞**韻約束（整段 `=`／whole-word equals）時，用既有按 (字數, 字位, 韻母) 嘅 runtime 音位索引：每位將參考韻母經 **韻母分組表** 展開後多 key 聯集，再跨位交集；**唔**為此增大 **詞條庫** 磁碟。**正韻** 整詞維持精準韻母序列等值。第一期只覆蓋呢條；單錨放寬已用同類展開。索引**未就緒**時回退全字數掃描 + 分組過濾（正確優先；index 好即用併查）；唔為快截斷池、唔阻塞等 index、唔因未就緒空白結果。**Desktop**（舊稱 Portable；本機服務＋word_cache 音位索引）與 **PWA**（瀏覽器 phoneme index）行為契約一致、命中集合一致；實作各接各 index。併查**只**收窄韻維；**碼約束**其後用既有聲調精度／碼鬆檔過濾（唔把碼 digit bake 進音位索引鍵）。排序維持 **韻母比對檔排序**（正韻層優先）。
_Avoid_：放寬整詞仍默認掃晒字數桶當唯一正路、把分組 id bake 入 lyrics.db 主表當第一期方案、index 未好就 LIMIT 截斷候選宇宙、未就緒就拒查或無限等 index、只優化一端令 Desktop／PWA 結果分叉、為碼加速把 code 維度打入 phoneme index 鍵（第一期）

**韻母比對檔排序**：
當 **韻母比對檔** 唔係 **正韻** 時，命中集合仍係該檔下放寬後嘅合格集，但排序多一層硬鍵：**正韻命中** 永遠排在僅靠通韻／腹韻／尾韻放寬先入圍者之前；層內再套用既有搜尋／句格排序（詞頻、關係提升等）。**正韻命中**＝詞級全有或全無：查詢內每一個受 **同韻** 約束嘅字位，都要對該位錨整段韻母一致；任一韻位只靠放寬入圍 → 成詞落放寬層。第一期只分兩層，唔做通韻＞腹韻＞尾韻瀑布，亦唔按「幾多位正韻」計分。檔＝**正韻** 時無第二層。
_Avoid_：放寬檔下同池無分層、結果 UI 強制切成兩大段列表（排序鍵即可）、第一期多級韻鬆緊瀑布、只睇詞末一個韻位定層、按位計分同詞頻鍵混戰

**同聲**：
兩個字面在對應音節位置上聲母一致；常以 **聲錨符** `^` 與參考字標示（如 `?^就`、`^香?`）。舊左 `=` 形（`?=就`）仍相容，見 ADR-0062。
_Avoid_：用「同音」泛指同韻或同聲、initial（實作名）；規範教學仍寫左 `=` 表同聲

**工作台選定讀音**：
**句格工作台**每個有多讀音字目前由創作者選定的**讀音**；工作台的連續**同韻**／**同聲**條件，只按各字位嘅**工作台選定讀音**組成一段比對，唔展開同一字面其他讀音再自由組合。創作者改選讀音後，候選須即時按新讀音重查。
_Avoid_：把字面所有讀音做笛卡兒組合、改選讀音但沿用舊候選、用權威預設讀音覆蓋創作者選擇

**工作台讀音投影**：
**句格工作台**把詞條庫既有單字讀音投影到逐字格嘅唯讀流程；Portable／PWA 皆先去重字面、批次查詢，再依原位置展開。adapter 以詞庫身份隔離 bounded LRU（含未收錄結果）並合併相同進行中請求；編輯 coordinator 取消舊請求，只為未有**工作台選定讀音**嘅新格補預設，唔覆蓋未改格嘅創作者選擇。純漢字**詞條 lookup**嘅記憶體臨時詞條生成不屬此流程。
_Avoid_：逐格查庫、讀音投影時注入詞條、React state 擁有跨頁快取、晚到結果覆蓋新編輯、把 lookup 臨時生成一併停用

**同音**：
在本工具中，指對應音節上的 **394052 碼**與查詢數字一致（**0243模式**下含等價變體）；不指整個音節聽感完全相同。**句格工作台**預設嘅碼約束即本義：碼來源檔為**同音**時，只對**字位鎖定**格注入該格碼（來自**工作台選定讀音**，或純碼起句該格輸入數字），再按當前**聲調精度**比對；**替換段**內未鎖定格、以及已鎖但尚未有碼嘅格，唔帶碼約束（通配；有碼後即時補上）。口語「暗合聲調」指同一行為。**搜尋列**唔做暗合：無明示數字位就唔加碼約束（如 `?困潦倒=` 只有韻錨；要碼須寫 `?4困=4潦=9倒=` 一類明示）。
_Avoid_：用「同音」泛指同韻或同聲；同碼（若 UI 已用「同音」則不必並列）；暗合聲調（作獨立正名；用**同音**）；宣稱搜尋列 `?詞=` 會暗合參考字聲調碼；鎖格未有碼就阻塞整段候選

**工作台碼約束檔**：
**句格工作台**替換條件上、與**聲調精度**正交嘅碼來源閉集：**同音**（預設；只鎖格帶碼）、**不限定**（段內無碼約束）、**指定碼**（創作者輸入長度＝段寬嘅數字串，可含 `?` 通配，覆蓋暗合來源；逐位生效，唔再論鎖定）。選**指定碼**時以單行控件編輯；切換進去時預填當時**同音**規則會產生嘅碼串（未鎖或未有碼之位填 `?`），只收 `0–9` 與 `?`；段寬變更則按新寬度重算／截斷預填。可與**同韻**／**同聲**同時生效（AND；唔因勾押韻而靜默改碼檔）。要對齊搜尋列純韻錨（如 `?困潦倒=`）時，碼檔改**不限定**再勾整段押韻。有替換段但無任何碼位、亦無同韻／同聲時：按現有音／碼／語意篩選誠實掃描完整字數候選池；語意 off 時全量歸入 `只合音格`，有語意種子時先排直接近義／語意相關，再列其餘候選。任何碼檔下，關係候選都須先符合當前碼／同韻／同聲條件，之後先喺分頁前按關係順位提升；唔准繞過條件，亦唔准因原本詞頻位置太後而喺首批消失。
_Avoid_：用「同音」泛指同韻或同聲；同碼（若 UI 已用「同音」則不必並列）；暗合聲調（作獨立正名；用**同音**）；把碼來源檔同聲調精度（m1／m2／m3）混成同一控件；**同音**檔下對段內未鎖格暗注入碼；勾同韻／同聲就自動切走**同音**；把工作台預設同音誤當成搜尋列亦暗合碼；指定碼空框起步或逐格散落輸入（第一期）；用單次大請求代替全量池嘅安全分頁；有啓用 POS 時把未知詞性誤當符合

**工作台候選掃描與 POS over-fetch**：全量池由後端／PWA planner 按固定排序合併、按字面去重後再切 `WORKBENCH_CANDIDATE_PAGE_SIZE=400` 頁；`engineTotal` 代表完整合併池，唔係當頁數。POS 篩選留喺候選 session：啓用時缺詞性載體不入選，每次 fetch 最多自動探測 5 頁（2,000 筆原始候選），未湊滿篩後目標就停並保留 `hasMore`，由創作者按「載入更多」繼續；未啓用 POS 時首批按需載入，唔預先灌完整詞庫。

**候選快照**：
**句格工作台**為同一候選查詢身份定格嘅完整候選池：固定排序合併、按字面去重、詞性篩選前；`engineTotal`、**擷取頁**與其後 POS 投影皆讀同一份，續頁期間唔重排。身份由會改變候選池嘅比對規格、模式、**韻母比對檔**、語意意圖／種子與詞庫／關係資料身份構成；POS、offset／limit、句稿版本唔屬身份。
_Avoid_：candidate cache（實作名）、把每個擷取頁當獨立候選宇宙、POS 切換或背景預載完成時原地重排、用句稿版本代替候選查詢身份、改韻母比對檔仍沿用舊快照

**工作台起句**：
**句格工作台**輸入列拆成逐格：純字面、純數字碼、純平仄串列，或**混合起句**（同一句內漢字格、數字碼格與**通配符格**並存；逐字元分流——漢字→字面格、數字→純碼格、`?`／`_`／`%`→通配符格，如 `能夠44`→四格、`?香??`→四格）。平仄串列第一期仍須整句純 `平／仄／P／Z`，唔同漢字、數字或通配混寫。
_Avoid_：把完整 0243 搜尋語法（等號錨、sandwich 等）當起句語法；第一期允許平仄與漢字／數字／通配混串；拒收合法混合起句為「mixed」；工作台拒收搜尋已認可嘅通配符

**通配符格**：
**句格工作台**句面上字面不限嘅一格；輸入 `?`／`_`／`%`（與搜尋**通配符**等價），畫布統一顯示 `?`。可**字位鎖定**以標定**替換段**；可與碼約束正交（同音／指定碼仍可約束該位碼）。唔等於**指定碼**入面嘅碼位 `?`。
_Avoid_：把通配符格當空白拒鎖、把指定碼 `?` 同句面通配符格混成同一狀態、通配符格自動清碼

**字位鎖定**：
**句格工作台**中，創作者**單擊**字位以標定**替換段**端點（**即時**生效，唔用延遲佇列；滑鼠在 `pointerdown` 鎖、觸控在短距 `pointerup` 鎖，避免捲動誤觸；候選查詢用 deferred plan，唔擋鎖格 paint）。格上有**字面**、**通配符格**或有碼先可鎖（純碼格可鎖；真正無字無碼無通配空白拒鎖）。**唔**再把鎖定格自動變成必須保留嘅字面約束。**雙擊**（或焦點格 Enter／F2）進入**單格手改**：一次只改一格，內容限一個漢字、一位 `0–9` 碼、或一個通配符；Enter／失焦確認，Esc 取消；空或非法唔改。同一格短窗內第二下視為雙擊前奏、**唔**再 toggle（避免鎖完即解鎖）；跨格連續單擊各自即時鎖定。與**詞庫勘誤**以 (字面, 舊粵拼)「鎖定目標」無關。
_Avoid_：圈選（舊獨立選段）、把鎖定當字面釘死、同詞庫勘誤鎖定目標混稱、要求純碼格必須先有字面先可鎖、單擊即改字面、拒鎖通配符格、連點多格時漏鎖中間格、用全域延遲鎖定換雙擊分界、pointer 鎖後再被 click 雙重 toggle、觸控滑動仍當鎖

**清鎖定**：
**句格工作台**一次解除句內全部**字位鎖定**並撤銷當前**替換段**（`selection` 空）；**保留**句面字面／讀音／碼與替換條件控件。**唔**等同**清空句格**；**唔**入**句稿復原**（同單擊鎖／解鎖）。無任何鎖定時控件不可用。控件位置：畫布標題列、**段手打**（✎）左側；與**清空句格**掣分開。
_Avoid_：把清鎖定當清空句格、為清鎖定寫 undo、清鎖定時重設句面或碼檔、把清鎖定同清空句格排成易誤觸一組而無 disabled 狀態

**替換段**：
由至少一個**字位鎖定**派生嘅連續字位範圍（bounding span）；**寬度上限＝當句字位數**（與**工作台起句**一句上限相同，現行 64 格），**唔**另設「最多四格」硬頂。供一次候選替換；一次候選＝**一個等長字面**換晒成段（唔拼多詞）；段內各格均可換（約束來自聲調碼／同韻同聲等，唔來自鎖定本身）。無鎖定則無替換段。句格畫布右上角 **✎** 展開**段手打**（無鎖定則不可點）；規則同**工作台起句**（可含通配符格），長度須＝段寬；確認後自動解析讀音／碼並入**句稿復原**。闊段空池／何時跳過查詢等產品門檻見 [ADR-0069](docs/adr/0069-workbench-replacement-span-line-width.md)。
_Avoid_：圈選、選段（舊名）、把鎖定格當 literal 必留、把手打常駐條件列或常駐展開、把段寬另鎖死 4 格、闊段自動拆多詞拼段、把段寬開到超過一句上限

**韻／聲參考字串**：
**句格工作台**替換條件上，同韻、同聲各一條可選參考輸入（額外維度）。空白＝跟句格字面／**工作台選定讀音**（placeholder 示意跟原韻／跟原聲）。非空時長度須等於該維度**已勾位置數**（整段押韻／同聲＝段寬）；對應到已勾位置由左至右；串內通配＝該位退回句格；漢字位**覆蓋**句格字面作錨（外字取第一讀音）。有音位錨即可查候選，唔強制段內漢字種子。
_Avoid_：參考串永遠強制＝段寬並右補、有字面就禁止覆蓋、空白當非法、同韻同聲逼共用一欄、無種子就拒絕純押韻查詢

**句稿復原**：
**句格工作台**單層 undo：覆蓋「套用候選」「確認放寬」「段手打」「單格手改」、以及**清空句格**（還原整份句稿連替換條件控件）。**唔**為單擊鎖定／解鎖、**清鎖定**、或只改**工作台選定讀音**另開復原。有句稿時控件在**本次替換條件**面板；清空後喺「建立句格」旁出細掣「復原」（完整說明走 title／aria）；捷徑 U／Ctrl+Z。
_Avoid_：多層 undo 棧、為鎖定切換或清鎖定寫 undo、把復原掣只放在候選區底部、清空後留空白格佔位當「已清空」、清空後用長文案大 pill 當復原入口

**清空句格**：
**句格工作台**把現有句稿撤到同首次進入一樣：`draft` 空、畫布同條件／候選消失，只留起句輸入；並清除持久化句稿。可經**句稿復原**還原清空前狀態。**唔**等同只做**清鎖定**。
_Avoid_：清空後仍留 N 個空白格、只清字面而留碼格當完成清空、把清鎖定當清空句格

---

## 搜尋模式

**搜尋模式家族**：
頂層搜尋入口三分：**0243搜尋模式**、**平仄**（格律家族；旁註／readout 可寫「平仄模式」，唔等於 **平仄串列查詢**）、**近反義模式**。寬屏由頂欄切換；窄屏可由選單切換。唔等同 **0243搜尋模式** 旗下 **0243模式**／**02493模式**／**394052模式** 三檔。
_Avoid_：把家族同檔混稱「頂欄三粒」、PZ 模式、把 **平仄** 家族等同 **平仄串列查詢**

**0243搜尋模式**：
以 **394052 碼**／**02493 碼**為核心的搜尋；屬 **搜尋模式家族** 之一。旗下三檔並列：**0243模式（鬆）**、**02493模式（緊）**、**394052模式（六聲）**；預設 **0243模式**。三檔貼住搜尋列切換（唔佔家族頂欄位）。
_Avoid_：鬆／緊並稱作模式正名、把 0243搜尋模式當成近反義的同義詞、把三檔叫頂層入口

**單碼常用讀閘**：
純數字查詢且長度為 **1**（一個 digit）時：每個字面只以詞庫內該字**全部**單字讀音中 **pron_rank 最佳**（最低 rank 數值）嗰批讀音比對查詢碼（含當前檔鬆檔 variants）；命中後用**等號參考讀音選列**權威序揀一條展示。目的：避免罕見／棄用／錯誤讀音因碼碰巧相同而淹沒單碼結果。字只有冷讀時冷讀即為最佳，仍可出；`pron_rank` 未知與 MatchSpec preferred 契約相同。**唔**套用到多位純碼（`22` 等，靠 essay／**排序用 curated** 沉底）、缺字／工作台／同音異讀／純漢字 **詞條 lookup**。
_Avoid_：把多位純碼一併做 preferred 硬閘、只喺碼命中列內算「最佳」（會誤收罕見）、單碼結果展開同一字多讀音列、UI 另加「已隱藏冷僻」hint

**394052模式**：
**0243搜尋模式**最嚴檔（三檔之第三、URL `m3`）：查詢與比對皆在 **394052 碼**字母表內**逐位精確**匹配，唔套用鬆檔等價變體；三聲 `4` 與五聲 `5` 唔互通。**平仄串列查詢**轉接至此檔且唔出轉接提示。介面旁註顯示「六聲」。
_Avoid_：m3（作領域正名；僅程式與 URL）、六聲模式（作唯一正名時與 394052模式 重複）、矩陣（舊旁註；已改六聲）、稱三檔為頂欄家族入口

**0243模式**：
0243搜尋模式預設檔：查詢先正規化為 **394052 碼**，再允許**逐位**鬆檔等價變體（**`3↔9`**、**`2↔6`**、**`4↔5`** 等）。介面旁註顯示「鬆」。
_Avoid_：鬆模式（作領域正名）、m1（實作名，僅程式與 URL）、整段碼只做全串替換而漏交叉組合

**02493模式**：
0243搜尋模式進階檔：查詢經共用聲調矩陣正規化為 **394052 碼**；除 **`4↔5` 逐位鬆檔**外，**唔**套用 **0243模式**其餘鬆檔（如 `3` 與 `9` 唔互通）。介面旁註顯示「緊」。
_Avoid_：緊模式（作領域正名）、m2（實作名，僅程式與 URL）、9鍵／九鍵（應作 9 碼）

**近反義模式**：
以詞義關係為主的搜尋，回傳近義、反義與語意相關詞條。介面 pill 可簡寫「近反義」；旁註顯示「查」。教學與文件用全名。輸入**近反義關係查詢**語法時觸發**搜尋模式轉接**（見下），唔再空結果。
_Avoid_：syn、查模式（作為模式正名）、關係模式

**近反義前次0243搜尋模式**：
用戶切換至**近反義模式**之前所選的 **0243搜尋模式** 檔（**0243模式**、**02493模式**或 **394052模式**）；**搜尋模式轉接**時還原此檔執行**近反義關係查詢**。若無紀錄（如首次進站即在近反義），預設 **0243模式**。**API 保險轉接**時可由客戶端以 query param `fallback_0243_mode` 傳遞（`m1`／`m2`／`m3`）；同名 HTTP header 可選；皆缺則預設 **0243模式**。
_Avoid_：lastMode、m1/m2/m3（實作名）

**搜尋模式轉接**：
在**近反義模式**下，若查詢串屬**近反義關係查詢**語法，自動切換至**近反義前次0243搜尋模式**（無則 **0243模式**），更新頂欄模式與 URL，並以該檔執行查詢；純漢字池查詢（如「開心」）仍留喺近反義模式。轉接時重設分頁並顯示**轉接提示**。執行分兩層：**介面轉接**（送出前先切模式）與 **API 保險轉接**（後端改檔並回傳 `hint`／`X-Effective-Mode`）。決策單一出口：ModePolicy `plan_redirect`／`planRedirect`（should_redirect／effective_mode／hint／reset_offset）；detect 可走 full-parse 或 regex adapter，結果形狀相同。UI commit 經薄 SearchSession（mode／pzmode／fallback／hint／offset），唔吞 tabs／就緒閘。
_Avoid_：模式偷換（UI 仍顯示近反義）、把缺字／碼字查詢一併轉接、僅後端轉接而介面唔更新、轉接後沿用舊 offset、UI 與引擎各自拼 hint／fallback／offset

**轉接提示**：
**搜尋模式轉接**發生時，於結果區頂部顯示嘅一行說明（如「此語法已切換至 0243模式（鬆）查詢」）；與查無結果、粵拼在近反義模式被拒等既有 `hint` 同列，唔阻塞結果展示。**介面轉接**由前端本地生成；**API 保險轉接**由後端 `hint` 回傳；兩層文案一致。
_Avoid_：toast、彈窗確認

**語意相關**：
與查詢詞在語意上有關聯、但非近義亦非反義的詞條（近反義模式第三類結果）。存儲／API 枚舉可用 `semantic_related`；創作者文案用本正名。
_Avoid_：相關詞（不區分類型時）、把語意相關當近義或反義展示

**語意向量鄰居烘焙**：
**發佈主理**離線用固定 embedding 模型（現行目標：**bge-m3 ONNX**）為 **詞庫** 字面算 dense 向量，再預算每頭 top-K 語意鄰居，寫入兩類產物：（A）可進 release 嘅 **語意相關** 邊（`word_relations`，`source=embedding_cosine`，排序低於 manual／project／cilin／guotong）——**只補缺／少直連 syn 嘅頭**；（C）**全 DISTINCT** 提案 TSV（可升 **專案自建近義**，**唔**自動升反義）。Encode／top-K 宇宙可全庫；**唔**等於全庫都寫入交貨關係圖。**Runtime（Desktop／PWA）只讀表，唔載模型、唔現場算向量。** 向量 sidecar 可留主理機；**唔**把全庫向量塞入交貨 `lyrics.db`（交貨庫 embedding 欄維持清空）。Cosine 鄰居 **唔**自動標 `ant`（反義仍靠直連／**近義橋反義**／**專案自建反義**）。
_Avoid_：runtime 載 bge／ONNX、交貨庫存全庫向量、cosine top-K 直接當反義權威、用 embedding 取代 essay 詞頻排序、CPU silently 頂替已宣告嘅 GPU 烘焙、把全庫 C 提案無審直接灌 A

**語意鄰居 GPU 烘焙**：
**語意向量鄰居烘焙** 嘅 encode／全庫 top-K 相似度步驟 **必須** 用 GPU（CUDA EP 或等價）；啟動時偵測唔到可用 GPU → **失敗退出**，禁止默默改 CPU 跑完當成功。CPU 只可用於單位測試／極小 fixture，唔作全庫發佈路徑。
_Avoid_：全庫 CPU fallback 當正式 bake、無 GPU 仍寫 release 邊

**近反義池**：
合併 word_relations + 靜態詞林 + 衍生反義的近/反/語意候選（有效字面），排序去重。**近反義模式**與!共用同一 ants。**投影統一讀取入口**（`project_relation_pool`／`projectRelationPool`）；建池為內部。source 順位／runtime 衍生反義來源 id 以 `contracts/relation-pool-ranking.json` 為 SSOT（codegen 雙端）。DB 唔保證存晒 Cilin 葉組完全圖（可有鄰居度上限；其餘靠 **靜態詞林埠**）。`embedding_cosine` 語意相關邊屬池內低順位來源。
_Avoid_：relation pool、合併衍生與 runtime、舊快照當 SSOT、假設 DB 有無限 syn 鄰居、PWA shallow barrel re-export、runtime 繞過投影直呼 builder、兩邊手抄 SOURCE_BASE_RANK

**近反義池快照**：
單字面建池後定格（三池+計數）。供投影與分頁。
_Avoid_：掛 session、當可變上下文

**近反義池投影**：
近反義/`~`/`!`/關係補錄統一入口。! 直用 ants；反義讀 direct 子集。_Avoid_：獨立 expand、ingest 繞過投影

**靜態詞林埠**：
bundled 近/反義語料 raw lookup（cilin + guotong）。Runtime ~ / ! 消費；ingest 分源。授權閘：只納可再分發者。反義 guotong，唔 OpenHowNet。
_Avoid_：直接讀 dict、無授權入 bundle

**收錄決策**：
字面 raw lookup 上判定注入與讀音來源（rime/詞級/拼接）。參考字解析經此。
_Avoid_：admission、快取優先於決策

**參考字讀音解析**：
依決策解析參考字：**錨點選項**與**等號參考讀音**兩入口；呼叫端明示注入。
_Avoid_：隱式 ensure、把整段當條件、OR 多讀音

**錨點選項**：
參考字作韻／聲錨時可用嘅音素集合；多讀音取 union，但只含 **pron_rank** 為預設或常用嘅讀音，**唔含罕見／棄用**；**pron_rank 未知**嘅收錄讀音仍入 union。同音異讀查詢唔經此集合。
_Avoid_：等號參考讀音、全庫 OR、罕見／棄用讀音 union

**組詞讀音**：
同一字喺固定詞語入面嘅讀音，可與單字預設讀音唔同（如「潦」單字 liu2、成語「潦倒」入面 lou5）。
_Avoid_：單字讀音、預設讀音（當泛指組詞時）

**語境錨點選項**：
部分韻／聲錨（如四字部分韻錨）喺特定字位，除**錨點選項**外，另從同寬已收錄詞語掃描該字實際標音而得的音素集合；反映**組詞讀音**，**唔**用 pron_rank 剔罕見／棄用。
_Avoid_：錨點選項、單字 union

**等號參考讀音**：
等號與碼夾等號內嵌聲比對時，參考字嘅單列權威讀音；按 **pron_rank** 選列（平手再按詞頻等 tie-break）。
_Avoid_：錨點選項、多讀 union

**關係寫入**：
近/反義寫 word_relations。契約：小 id 前、唯一 (id, related, type)。每字面以最小詞條為錨。bulk insert 統一，ignore 衝突。
_Avoid_：merge pipeline、staging 依賴、雙管線

**關係直寫**：
build-word-relations 記憶體集 + bulk。build-db 熱路徑只此。
_Avoid_：staging、展開多讀音

**關係錨點詞條**：
每字面最小 word.id 為端點。與等號選列不同契約（圖以字面鄰）。
_Avoid_：當查詢權威、N×M 邊

**關係圖**：
已收錄字面直接近/反鄰居（word_relations + 靜態）。共用 lazy 快取。
_Avoid_：全量重建、就緒閘預熱

---

## 查詢語法

**查詢分派**：
將創作者輸入分類為等號、碼字、缺字等語法類型，正規化為可比對規格並路由至對應搜尋路徑；**分派優先序**本身是領域規則的一部分。執行層（**缺字型查詢執行**）不應重複語法解析或 ParsedQuery→規格轉換。
_Avoid_：parser、handler chain（實作細節）、在 position_match 各處散落 build_* 正規化

**查詢種類**：
`parse_query` 產出後，每條查詢歸一類（等號、串列韻／聲錨、前綴通配等號、詞條 lookup…）。**比對規格建構**與**搜尋路由**須以同一**查詢種類**驅動，唔再各自維護第二套型別分支；**parse_query** 優先序鏈可維持現狀。
_Avoid_：parse 後仍用 dataclass isinstance 梯做 spec／dispatch、把查詢種類當實作 enum 名寫進領域文案

**比對規格建構**：
語法家族 module 負責該家族嘅 `parse` 與 `to_match_spec`（ParsedQuery → **比對規格**）；查詢種類註冊表只做 kind→fn 委派，唔內嵌槽位／overlay 邏輯。樣板：近反義 relation、平仄、equals、serial、rhyme、plus、mask、wca、**jyutping**（`jyutping_anchor`；含 dual carrier）。heteronym 係 `match_spec: false`，只 parse＋獨立路由。
_Avoid_：registry 內再寫一份家族槽位規則、執行層重做 ParsedQuery→規格

**查詢語意解釋**：
創作者即時說明意圖；與 **搜尋教學**、**搜尋教學速覽** 同屬**創作者教學文案**面，用語須填詞人一眼睇明（見 **搜尋教學**）。**文案規則單一**（見 ADR-0021）。**比對規格**路徑演算法形狀：`build_match_spec` → **結構型 Explain IR**（字位索引、slot kind、dimension、ref_literal 等；唔夾「第 N 個字／雙押／同韻」creator copy）→ **render** 成摘要；lookup／近反義／平仄等短回路仍 ParsedQuery 直出最短句。位置易混 **warning** 仍 ParsedQuery 側。查詢**有** **同韻**／final 約束時，摘要須標明當前 **韻母比對檔**（正韻／通韻／腹韻／尾韻）；**無**同韻時唔提檔。**正韻** 下零結果可附次要 hint 試放寬檔；已放寬仍零結果唔再推更鬆。**Portable 與 PWA 各一解釋實作**（雙引擎）；IR schema 見 `contracts/query-explain-ir.schema.json`；parity 以 `query-explain-parity.json` 字串 contains + optional `ir_assert` 防漂移。
_Avoid_：格槽音節、第三套 parse／自創槽語、解釋當規格、「同音」泛同韻、押韻標用於逐字/疊韻句、只准一邊寫死而另一邊自由改 copy、標題／摘要用串列／通配／錨／遮罩／碼夾等號等技術譯名、IR 夾已組好 creator 片語、無韻錨仍常駐解釋檔名、放寬檔零結果仍循環提示再放寬

**押韻標註**：
等號/前綴等號句尾標參考詞字數（單/雙/三/四押...）。碼約束另補。
_Avoid_：通用韻腳標、與排序混、cap 四押

**缺字型查詢執行**：
查詢分派規格統一執行為搜尋結果。~~/!! 不屬此。候選抓取受 **候選來源截斷** 與 **語意完整候選宇宙** 約束。
_Avoid_：position match、執行層分派梯、所有搜尋一律取消截斷

**候選來源截斷**：
對缺字型候選抓取施加上限，以限制大長度桶／冷路徑成本。只可喺截斷後**唔會**再因語意約束剔走命中時使用；純碼／純形狀且無語意後置約束時可截。
_Avoid_：結果分頁上限、把純碼大池截斷當語意漏命中 bug、unlimited／全桶（實作用語）

**語意完整候選宇宙**：
套用語意後置約束之前必須完整嘅候選集合。凡截斷後仍會因語意約束剔詞者，必須用此宇宙，唔准先截再濾。目前包括：粵拼字母槽、聲韻錨、等號／碼夾 span（含僅音位錨）。新語法家族預設要過呢條，唔係預設截斷。
_Avoid_：以為有全碼約束先至要完整宇宙、與結果分頁混淆、把實作 flag 名當領域名

**近反義關係查詢**：
0243模式下 ~ / ! 查近/反義。可前綴碼。單字種子經反義複合補。!!/~~ 複合；!{連}! / ~{連}~ 連接詞複合。模式輸入觸發轉接。符號正規化。`!` 直用 **近反義池** ants：字面 ∈ **詞庫** 但無任何可落地 **直連反義**／靜態可入圖 ant 時，空結果係預期（反義關係缺口），**唔**等如缺詞。
_Avoid_：稱為開啟近反義模式、把「! 無結果」一律歸因詞庫字面未收


**反義/近義複合詞**：
填詞二字對舉（!! / ~~）。curated ∩ 詞庫 → 素掃描 → 單字合成。快照供離線預載。
_Avoid_：一般雙字詞

**連接詞複合查詢**：
三字，中格固定連接詞（與和或…）。首尾互為近/反（對應 ~~／!! 規則）。**查詢種類**獨立為 `compound_connect_syn`／`compound_connect_ant`（唔再與 `~~`／`!!` 的 `compound_syn`／`compound_ant` 共用 kind）。候選＝詞庫已有三字 ∪ 由雙字複合 flank 合成（缺庫者拼單字＋連接詞讀音）；詞庫優先、合成殿後。~ 與 ! 嚴格互斥（**ant-wins**：反義對唔入 ~；! 保留完整反義 primary）；flank 雙向（AB 可出 A連B 與 B連A）。雙端一致。**Portable** 搜尋請求路徑合成結果可只在記憶體上榜，唔為合成寫入詞條庫（免鎖庫拖垮並行查詢）。
_Avoid_：任意虛詞、當整詞 lookup、~ 混入反義對、只掃詞庫而當數量應等於 ~~、請求路徑逐條寫庫合成、用 dataclass isinstance 分辨連接詞與 ~~／!!

**源 3 tier 快取**：
~~ lazy 單字近義合成 tier。效能用。
_Avoid_：當快照

**直連反義**：
`word_relations` 上非衍生來源嘅 ant 邊，外加 **靜態詞林埠** 反義 lookup。衍生來源（近義橋、runtime 詞林衍生／鏡射、`manual_ant_mirror` 等）**唔**算直連。`manual`、guotong、以及 **專案自建反義** 屬直連。
_Avoid_：把近義橋／衍生當直連、用「有任何 ant 列」等同直連

**直連近義**：
`word_relations` 上非衍生來源嘅 syn 邊，外加 **靜態詞林埠** 近義 lookup（cilin／guotong syn）。`manual` 與 **專案自建近義** 屬直連。句格工作台「直接近義」讀近反義池 syns，即呢類。
_Avoid_：語意相關當直連近義、把 cilin 葉組完全圖當成每頭都有直連

**有近無直連反**：
字面喺關係圖有近義，但無 **直連反義**（可仍有近義橋或衍生反義）。**專案自建反義** 種子契約用此；異於 **近義橋反義** 目標集（無任何 ant 列）。
_Avoid_：有近無反（作自建種子正名；該說法易同近義橋目標混淆）

**直連近義過稀**：
字面嘅 **直連近義** 尾數 **＜2**（兩端 ∈ **詞庫字面集**）。**專案自建近義** campaign 種子用此；異於「完全無 syn 邊」或「有邊但質差」嘅口語說法。
_Avoid_：有近無反／無近義（作過稀正名）、未計量就當過稀、另開第二套「直連」定義

**專案自建近反義**：
**專案自建近義** 與 **專案自建反義** 兩份維護者審定資產嘅創作者／NOTICE 可見統稱；同一維護資料夾與單一 maintainer CLI 入口管理。領域實體仍係兩份清單，**唔**等於 runtime **近反義池**。
_Avoid_：近反義詞庫、自建反義詞庫（作統稱正名）、把統稱當第三份合併 SSOT、與近反義池混稱

**專案自建反義**：
維護者審定後寫入關係圖嘅 **直連反義** 邊（可經生成式 AI 起草）；權威係 git 追蹤嘅 **專案自建反義清單**；由關係建置套入 `word_relations`（專案 source），**唔**寫入 **靜態詞林埠** 檔（guotong／cilin）。與 guotong **並存**。近反義池去重優先直連；自建排序 **不低於** guotong。長遠直連覆蓋足夠可另閘硬取代近義橋／runtime 衍生；未過閘前橋與衍生作永久保底。本期產品面係資料＋授權（NOTICE／關於頁並存說明）；**唔**新開搜尋模式或教學語法。與 **專案自建近義** 平行；關於頁統稱見 **專案自建近反義**。
_Avoid_：project_llm（實作 source 名）、把未審定模型輸出當權威、稱作關係補錄、改寫 dict_antonym／cilin 冒充上游、只進 Release 無 git 清單、未過覆蓋閘就關橋／衍生、自建永遠低於 guotong 卻宣稱已升主源、把本期做成線上「一鍵生成反義」產品

**專案自建反義清單**：
git 追蹤嘅已審定 ant 對表（`data/syn_ant/project_antonyms.tsv`；與 **專案自建近義清單** 同分維護樹／CLI，可遷入 `data/syn_ant/project/`）；只含維護者接受嘅 **直連反義**。模型 raw 提案可另存、唔當權威。生成時每頭提案最多 **3** 條；審定入清單每頭最多 **5** 條。入清單前頭尾皆須 ∈ **詞庫字面集**（經 **詞條從源重建** 後之詞庫成員資格即可；**唔**要求同時過 **詞庫發佈閘**／Release）；若該對已存在近義邊（任一來源）則硬拒。禁止「清單先、庫後」過渡態：正式清單**唔**收 pending／待 membership 列；缺端須先經 **詞級標音 curated**（可單獨先合入）再重建過閘，對先可入帳。同批可**對級拆出**：已過 membership 者入本批；待補庫者留草稿／提案（人工跟進），另開小批入清單，唔拖整批、唔用同一 batch 拖延 append。清單完成（可 `accepted`）與執行期可見（關係套入 `word_relations` 後）刻意分開。清單可存生成方向單向列；ingest 套入時 **canonicalize** 成 `word_relations` 單一無向列（兩端查詢可見），唔寫雙列。批次 meta 見 `project_antonyms.meta.json`。首批（及往後擴大批）commit 前須抽樣閘（見實作計劃品質閘）。
_Avoid_：把 raw 生成檔當 SSOT、與 **關係補錄清單** 或 **專案自建近義清單** 混檔、用 pending／applied／pending_lexicon 狀態機、另開待庫權威佇列表、首批就追近義橋每頭 30 嘅密度、庫外字面入關係圖、自建自動蓋寫近義、同 pair 寫兩列 provenance、把入清單綁死發佈詞庫或當場重烘焙近反義快照、把草稿審定當終局 `accepted`

**專案自建反義種子**：
由 **有近無直連反** 字面按 **Essay 詞頻** 降序截斷嘅 **Top-K** 集合（K 為批次參數，寫入批次 meta；可重跑對照）。首批預設 **K=500**。種子與候選字面皆跟近反義 **有效字面** 規則（最長 12），唔另限單字。唔用絕對詞頻門檻作預設切法。
_Avoid_：全庫有近無直連反一次生成、以 Essay 作收錄門檻、同近義橋「無任何 ant」種子集混稱、另開第二套字面長度契約

**專案自建反義起草**：
用生成式 AI 為 **專案自建反義種子** 提候選時採 **盲生成**：prompt 以種子字面為主，可附該頭之近義鄰作語境；**禁止**將 guotong 或其他第三方反義全文／逐頭答案作 few-shot 或擴寫來源。生成後可對 guotong 做重合率統計，只作品質監控。
_Avoid_：以 guotong 作正負例 few-shot、先複製上游反義再 LLM 潤飾、把重合率當「准許複製」

**專案自建近義**：
維護者審定後寫入關係圖嘅 **直連近義** 邊（可經生成式 AI 起草）；權威係 git 追蹤嘅 **專案自建近義清單**；由關係建置套入 `word_relations`（專案 source），**唔**寫入 **靜態詞林埠** 檔。與 cilin／guotong syn **並存**；近反義池去重時自建排序 **不低於** cilin／guotong；**唔**硬隱藏上游 syn。產品面係資料＋授權；**唔**新開搜尋模式或教學語法。與 **專案自建反義** 平行；關於頁統稱見 **專案自建近反義**。
_Avoid_：project_syn（實作 source 名當領域正名）、把未審定模型輸出當權威、稱作關係補錄、改寫 cilin／guotong 冒充上游、自建永遠低於上游卻宣稱已壓雜訊、硬覆蓋只顯示自建尾、COW／MOE 當自建近義源

**專案自建近義清單**：
git 追蹤嘅已審定 syn 對表（`data/syn_ant/project/project_synonyms.tsv`＋meta；與 **專案自建反義清單** 同分維護樹／CLI）。契約鏡射反義清單：起草每頭最多 **3**、入清單每頭最多 **5**；頭尾皆須 ∈ **詞庫字面集**；與任一 **直連反義**（含 **專案自建反義**）無向對 **硬拒**；對已有上游 syn 邊允許 **軟重叠**入帳（meta 可標 overlap），靠排序抬自建。正式清單唔收 pending／待 membership。ingest canonicalize 成單一無向列。
_Avoid_：與反義清單混檔、硬去重禁止重叠以致無法抬升質差邊、庫外字面入圖、把 raw 生成當 SSOT

**專案自建近義種子**：
**直連近義過稀** 字面按 **Essay 詞頻** 降序截斷嘅集合（campaign freeze 用 Top-5000 或 len=4 母體；批次 Top-K 可另參）。有效字面最長 12。推進：先量度 freeze 報告，再 **高頻近義 campaign**，其後 **四字缺直連近義 campaign**。
_Avoid_：全庫一次生成、以「有近無直連反」反義母體重當近義種子、未量度就平行開兩大 campaign

**專案自建近義起草**：
為 **專案自建近義種子** 盲生成：prompt 以種子字面為主，可附該頭已有 syn 鄰作語境；**禁止**將 cilin／guotong 或其他第三方近義全文／逐頭答案作 few-shot 或擴寫來源。重叠率只作品質監控。
_Avoid_：以 cilin 候選當草稿再剔（非盲）、先複製上游近義再 LLM 潤飾

**高頻近義 campaign**：
一次凍結嘅 Essay Top-5000 **直連近義過稀** 目標集（邏輯排除已有 **專案自建近義**；與 len4 母體字面去重時本集優先）。每個 **campaign 目標詞** 須有近義側 **終局判定**。完成前唔以「高頻近義全 resolved」作發佈閘，除非另訂。
_Avoid_：叫成「常用近義詞庫完成」、與高頻反義 freeze 混帳、未量度過稀基線就開工

**四字缺直連近義 campaign**：
一次凍結嘅 **直連近義過稀** 且字面長度為 4、按 **Essay 詞頻** 降序截斷 Top-5000 的目標集（len=4 **proxy**，**唔**宣稱真成語；邏輯排除已有 **專案自建近義** 與 **高頻近義 campaign** 已佔字面）。全庫 len4 過稀遠大於 5000 時仍只 freeze 本 Top-K，唔一次清算。完成＝該母體全數已裁定。品質閘 ≥90%。產品面仍只清單＋套入。
_Avoid_：成語 campaign、全四字／全成語有近義、與高頻近義 freeze 混帳、把全庫 len4 過稀當本 campaign 母體

**adequate_existing**：
近義 campaign 第三終局：目標詞已有恰好 1 條填詞可用之 **直連近義**，維護者確認無需再自建補尾。須過抽樣閘；與 `accepted`／`no_natural_synonym` 互斥。
_Avoid_：把雜訊上游邊當夠用、用此終局於直連尾數＝0 嘅頭、反義 campaign 套用此終局

**高頻反義 campaign**：
一次凍結嘅 Essay Top-5000 **有近無直連反** 目標集（邏輯排除已有 **專案自建反義** 以免母體滑動）；每個 **campaign 目標詞** 最終須有 **終局判定**。完成前唔發佈以「高頻全 resolved」為閘嘅版本。
_Avoid_：把 campaign 叫「種子」、與單批 Top-K 種子匯出混稱、未完成就當 v1.0.9 已過完整詞庫門檻

**四字缺直連反義 campaign**：
一次凍結嘅 **有近無直連反** 且字面長度為 4 的目標集（邏輯排除已有 **專案自建反義**；與 **高頻反義 campaign** 平行、唔混同一 freeze 帳）。每個 **campaign 目標詞** 須有 **終局判定**；完成＝該母體全數 **已裁定**，**唔**宣稱「全部成語／全部四字都有反義」。未裁定頭按 **Essay 詞頻** 降序分批推進。他 campaign 已入帳之 `no_natural_antonym` 對同字面 **繼承為已裁定**（唔強制重審；翻案僅維護者手動）。起草用四字專用盲生成稿（契約同 **專案自建反義起草**，另偏好見 **四字尾軟偏好**）。品質閘同其他新 campaign 批（≥90%）。產品面仍只係清單＋套入；**唔**因本 campaign 完成而關閉近義橋／runtime 衍生，亦 **唔**改近反義池 ant 排序。
_Avoid_：叫成「成語 campaign」或「全四字／全成語有反義」、與高頻 Top-5000 freeze 混帳、把完成當全局直連覆蓋閘、runtime 為四字另訂排序、不繼承舊 no-natural 又重跑全量、MOE 相反詞當本 campaign 源

**四字尾軟偏好**：
**四字缺直連反義 campaign** 起草與審定時，優先收同長（四字）反義尾；短尾仍可入 **專案自建反義清單**（有效字面契約不變），每頭多候選時優先保留四字尾。唔因「只有短尾」改判 `no_natural_antonym`。
_Avoid_：硬性要求至少一條四字尾先算 accepted、改有效字面長度上限、把軟偏好做成 runtime 硬分層

**campaign 目標詞**：
凍結 manifest 內一個 head（固定 rank 與 batch 槽）；進度與完成閘只對此集合計數。
_Avoid_：把清單 tail、庫外字面、或非 manifest 字面當目標

**終局判定**：
每個 **campaign 目標詞** 恰好一個可稽核收斂。反義 campaign：`accepted`（**專案自建反義清單** 無向覆蓋該字面）或 `no_natural_antonym`（確認無自然、同詞性、同義層 context-free 反義）。近義 campaign：`accepted`（**專案自建近義清單** 覆蓋）或 `no_natural_synonym`（確認無可補之自然近義），直連尾數恰為 1 時可裁 **adequate_existing**。同側終局互斥。已審有合格對但因缺端待 **詞級標音 curated**／重建、對尚未入正式清單期間，維持**未裁定**。
_Avoid_：pending／deferred／accepted_pending_lexicon 當完成、把硬過濾 reject 當 no-natural、兩終局並存於同一目標詞、以草稿審定搶先 `accepted`、反義側濫用 adequate_existing

**能配則配**：
對未裁定 **campaign 目標詞**：若有可過品質閘嘅自然、同詞性、同義層 context-free 反義／近義 → 入帳 `accepted`；否則裁定對應 no-natural（近義側已有 1 條可用直連者可裁 **adequate_existing**）。唔為覆蓋硬砌弱邊，亦唔用配額湊 accepted。缺端須補庫先入帳者，對入清單前頭保持未裁定。
_Avoid_：先全判 no-natural 再回頭補、為達比例放寬 A–D

**no-natural 品質閘**：
對準備入帳嘅 `no_natural_antonym` 列抽樣；單位係 head＋reason。樣本須同時：(1) reason 屬受控清單且貼合該頭；(2) 確認無自然、同詞性、同義層 context-free 反義；(3) 唔應改判 `accepted`。樣本數公式同 pair 閘；新 campaign 批門檻 ≥90%，舊已過閘批可自述較低門檻。
_Avoid_：把 allowlist 格式通過當語意 OK、同 pair A–D 硬套、無 meta 抽樣就當批完成

**已裁定／未裁定**：
**已裁定（resolved）**＝該目標詞已有 **終局判定**；**未裁定（unresolved）**＝尚無（含已審對但待補庫／未入清單）。campaign 完成＝全部已裁定且無終局衝突。
_Avoid_：用 DB 列數或種子退出數代替已裁定計數、把待 membership 草稿當已裁定

**campaign 最終稽核**：
某一 campaign（反義：**高頻反義 campaign**／**四字缺直連反義 campaign**；近義：**高頻近義 campaign**／**四字缺直連近義 campaign** 等）全部 **已裁定** 之後，對該 campaign 的各終局（含近義側 **adequate_existing**）按 batch 分層重抽並過 ≥90% 閘嘅收官品質稽核；紀錄獨立於單批 meta。
_Avoid_：把單批 sample 當最終稽核、未全部已裁定就當已過最終閘、用高頻收官代替四字收官（或相反）、用反義收官代替近義收官

**近義橋反義**：
有近無反字面（無任何 ant 列）→ 語意向量找橋接近義（達門檻）→ 多橋合併借 **直連反義**（投影讀，排序橋分）。快照注入，僅 ingest。
_Avoid_：runtime 猜、預設 embedding、ingest 繞投影、同 **有近無直連反** 混作同一種子集

**詞林衍生反義 / 反義端點鏡射**：
**直連反義** 頭經靜態 cilin 近鄰或全源近鄰再展開反。per-head，查詢時展開，標衍生語意。快照可選。
_Avoid_：全庫 live、納未收錄、bake 與 runtime 雙軌

**衍生反義快照重烘焙 / 近義橋重跑**：
maintainer 單命令重烘焙（raw 變或演算法變）。預設 export-only。
_Avoid_：PR 預設跑、build 熱路徑 live

**橋接語意門檻 / 借入上限 / 橋分 / 多橋合併 / 目標排除**：
橋接門檻、每頭上限、排序依橋分、去重截斷。已反義字面排除。
_Avoid_：上限當門檻、單橋贏者、開跑快照判定

**創作者手動關係**：
**關係補錄**寫入 direct `word_relations`（`source=manual`）並一跳擴展（`manual_syn_cluster`／`manual_ant_mirror`）；擴展互斥：唔同時寫入種子字面既有相反關係類型。**創作者**填詞時**即時寫本機 **詞條庫****（唔經 rebuild）；**維護者**將要進 Release 嘅補錄寫入 **關係補錄清單**，由 **詞條庫建置命令** 套用。
_Avoid_：手動關係（作領域正名）、稱作近義橋反義、創作者補錄只寫 TSV 要等 rebuild、把本機 DB 補錄當 Release 權威

**撤回創作者手動關係**：
**創作者**可即時撤回本機 DB 列。**維護者**從 **關係補錄清單** 刪除或標記撤回；下次 **詞條從源重建** 或 **關係補錄熱套用** 生效。唔改 ingest 快照或 **近義橋反義快照**。
_Avoid_：撤回 ingest 關係、維護者只改 DB 唔改清單

**關係補錄清單**：
git 追蹤嘅 **關係補錄** declarative 檔（如 `data/relations/manual_relations.tsv`）；只載 `source=manual` **直連**列；一跳擴展（`manual_syn_cluster`／`manual_ant_mirror`）唔寫入清單、由套用時重算。由 **詞條庫建置命令** 尾段或 **關係補錄熱套用** 套用。只影響 Release **詞條庫**；**唔**取代創作者本機即時補錄。既有 Release／檢驗用庫可經 **關係補錄命令** 匯出直連回填清單。
_Avoid_：把清單當 upstream raw、用 `pending`／`applied` 狀態機、與 **詞庫勘誤** 混檔、要求創作者寫清單先至查到、把未匯出之本機庫當發佈 SSOT、把 cluster／mirror 列當清單 SSOT


**關係補錄介面**：
創作者手動關係專用 view；**查詢分頁**承載於**查韻介面**。創作者操作**即時寫本機 **詞條庫****；維護者將 curated 補錄同步寫入 **關係補錄清單** 以納入下次 Release。
_Avoid_：獨立 relation-entry 頁、雙軌 HTML、創作者操作只寫 TSV

**關係補錄命令**：
套用或檢查 **關係補錄清單**；補錄或撤回；共用字面驗證與一跳擴展計畫。
_Avoid_：manual command（作泛稱）、把本機即時補錄當發佈權威

**關係補錄熱套用**：
維護者對**現成** **詞條庫** 清掉 `manual`／`manual_syn_cluster`／`manual_ant_mirror` 後，再由 **關係補錄清單** 全量重套；**唔** wipe 詞條、**唔**重跑 **詞條從源重建**。權威仍係清單；套用後跟既有 **全量發佈**／詞庫換版（唔另開「只換關係分包」發佈面），PWA 先更新。雲端即時同步創作者本機補錄（多用戶／帳號）留待將來，唔屬本期。
_Avoid_：熱套用唔更新清單只改 DB、當詞條增量 patch、當創作者雲端同步、跳過發佈直接改所有用戶 PWA、另開只換關係包嘅發佈層

**orphan 關係補錄合併**：
**詞條從源重建** **wipe 前**必先把現行庫裡未入 **關係補錄清單** 嘅 `source=manual` **直連**（無向去重）append 入清單；清單已齊則 no-op（CI／乾淨機）。`manual_syn_cluster`／`manual_ant_mirror` **唔**合併入清單。之後先 wipe／重建／尾段熱套用。合併寫入後仍須人工審 commit；唔當默認已發佈。
_Avoid_：wipe 後先合併、把擴展邊寫入清單、跳過合併當無 orphan、自動 push 清單


**詞庫勘誤**：
維護者維護嘅 **詞條** 標音修正 overlay（declarative）；每次 **詞條從源重建** 必套用，優先序凌駕所有 **詞條 SSOT 來源**。以 **(字面, 舊粵拼)** 鎖定目標，**可選** **舊 0243 碼** 作第三把鎖（`set_code` **必填**）；在 SSOT 合併**之後**、寫入 **詞條庫** **之前**介入。TSV 欄：`char`、`old_jyutping`、`old_code`（可選）、`action`、`value`、`note`；**唔**含 `status`／`applied_at`。支援 `set_jyutping`（改讀音、連帶重算 0243 碼）、`set_code`（粵拼已對只改碼）、`delete`（刪該 canonical 讀音）。**唔**改 raw 檔、**唔**以狀態機決定是否套用、**唔**負責 upstream 無字面嘅**新增收錄**（見 **詞級標音 curated**）。與**關係補錄**（近義／反義）無關。
_Avoid_：把詞庫勘誤當關係補錄、只靠字面改多讀音列、用 `applied` 當權威、用勘誤 TSV bulk 補缺詞、逐源 overlay（除非另定源級審計）、`set_code` 唔鎖舊碼

**詞庫勘誤介面**：
維護者專用 **查詢分頁**；**查韻介面**搜尋框輸入 `debug` 開啟，**唔**出現喺一般導覽。填詞時把標音問題寫入 **詞庫勘誤** TSV（字面＋舊粵拼＋可選舊 0243 碼），**唔**即時改 **詞條庫**；生效須經 **詞條從源重建**。與 **關係補錄** 無關。
_Avoid_：把詞庫勘誤介面當關係補錄、創作者-facing 入口、即時改詞條庫、debug URL 門檻

**詞庫勘誤命令**：
觸發或檢查 **詞條從源重建** 流程中 overlay 段；`set_jyutping` 或 `set_code`；`delete` 刪錯列。**唔**再批次 `apply` 改現行 **詞條庫** 而跳過 rebuild。
_Avoid_：勘誤 action（作泛稱）、把重算碼叫改粵拼、獨立 apply 當權威路徑

**詞級標音 curated**：
**詞條 SSOT 來源** 之一：maintainer 維護嘅 **詞級標音** 小表，優先序在 words.hk／開放詞典之後，僅補前序仍無之字面。可 **新增收錄**；**詞庫勘誤** overlay **唔**負責新增缺詞。為補 **專案自建反義** 缺端而收錄時，用**同一套**標音／有效字面閘，**唔**另設反義專用放寬；可單獨先合入（唔綁同 commit 之反義清單）。反義對其後放棄或永不入清單，只要字面仍合閘即可留（唔因反義沒落地強制刪）；差字面按普通 curated 回滾。**收錄 ≠ 排序抬升**：唔自動寫入 **排序用 curated**；碼搜沉底（Essay 無／低）係預期。
_Avoid_：把**排序用 curated**同本檔混為一檔、用 CC raw 自動填表、數千條無審核 bulk 匯入、新增缺詞寫入勘誤 TSV、Beta 前一次 diff 自動灌入 legacy 讀音、為趕反義入帳放寬粵拼／碼、把 curated 當反義 pending 暫存而設自動刪期、收錄即當常用加分

### 查詢語法基礎

**參考字**：
查詢語法中標定某音節位置的漢字，用於同韻、同聲或字面約束（如 `23就` 的「就」、`2^我3` 的「我」）。
_Avoid_：錨字、錨點字

**等號（韻）**：
`=` 緊貼**參考字後** → 比**韻母**（`就=`、`香港=`、`04困=49倒=`）。純字面無 `=`／`^` 仍係 **詞條 lookup**。**唯一可省**：單錨**碼夾等號** `{碼}{字}` normalize 補尾 `=`（`23就`→`23就=`）；串列唔省。口訣與**加號錨**、**串列**、**等號查詢**、**碼夾等號查詢**共用。見 ADR-0062。
_Avoid_：把 `=` 當字面分隔符、離開參考字單獨寫 `=`、用左 `=` 教同聲（改用**聲錨符**）、宣稱純字面即同韻搜、串列省 `=`

**聲錨符**：
`^` 緊貼**參考字前** → 比**聲母**（`^就`、`^香港`、`?^就`、`2^我3`、`04^困49^倒`）。多字＝逐字聲母。舊左 `=` 同聲形（`=就`、`?=就`、`2=我3`、`04=困49=倒`）為 **silent 輸入別名**（仍解析；normalize／解釋偏向 `^`）；**搜尋教學**只教 `^`。見 ADR-0062。
_Avoid_：用 `>`／`<`、把 `^` 當字面、規範文案繼續主推左 `=` 表聲

**等號（韻／聲）**：
舊傘名。已拆為 **等號（韻）** 與 **聲錨符**；唔再作規範口訣。
_Avoid_：新文案繼續用此傘名教「前聲後韻」

**碼夾等號查詢**：
0243 碼夾住**參考字**、並以 **等號（韻）**／**聲錨符** 標示讀音維度嘅查詢；涵蓋 `{碼}{字}`（normalize 補尾 `=` → `{碼}{字}=`）、`{碼}{字}=`、`{碼}^{字}{碼}` 等（如 `23就`／`23就=`、`2^我3`；雙字尾韻例 `32就起`／`32就起=`，唔用 `12就起` 作規範示範）。舊內嵌聲表面 `2=我3` 仍相容。左碼位數須與詞長一致。**比對規格**上每位 0243 碼為獨立格約束（串列逐格鬆檔；**0243模式**）；唔以整段「全碼字串」做第二套變體規則。**尾韻**（`字=`）可比該格字面或同韻，參考字韻母用**錨點選項**；**內嵌聲**（`^字`）比聲母，參考字用**等號參考讀音**。與**整詞等號查詢**（如 `香港=`）及**串列韻／聲錨**（多錨，如 `04困=49倒=`／`04^困49^倒`）分開。
_Avoid_：碼字查詢（作獨立家族）、hybrid code

**位置錨定**：
同一符號（**slot 連接符** `+`、**等號（韻）** `=`、**聲錨符** `^`、粵拼字母片段）喺查詢中**唔同字位**可以有唔同意義，係創作者最易混淆嘅根源；**加號錨**、**韻／聲錨**、**粵拼錨**、**串列韻／聲錨**皆屬此族。典型易混：`23o`（兩個字，末字同韻母 o）與 `23+o`（三個字，第 3 個字同韻母 o）；聲母對偶 `34p`／`34+p`（末字聲母）與 `3+p4`≡`3?p4`（中格聲母；`gw`／`kw` 同；`ng`／`m` 唔走呢條）。
_Avoid_：把位置錨定當實作術語寫入創作者解釋、用「格」「槽」描述字位（解釋層改用**第 N 個字**）

**通配符**：
`?`、`_`、`%` 三者等價，表示該格**字面不限**。用於缺字各格及韻／聲錨、粵拼錨前後格；只約束字面，唔決定讀音。
_Avoid_：把參考字或粵拼錨當通配符

**音節槽**：
詞長由左至右掃描決定。**0243 碼**每位一音節；**通配符**佔一槽；**slot 連接符** `+` 表多一槽（句首 `+` 例外見 **slot 連接符**；**`?` 緊接 `+` 再接錨**時 `+` 唔另加槽，見下）。例：`就=` → 1 槽；`?就=`／`?+就=` → 2 槽（等價）。**查詢語意解釋**面向創作者時改說**第 N 個字**，唔直接講槽。
_Avoid_：用串長代替掃描、把 `?就=` 當單格（單格用 `就=`）、把音節槽寫入創作者解釋文案

**slot 連接符**：
規範形 `+`；半形 `*`、全形 `＊`／`﹡`、全形 `＋` 為 **silent 輸入別名**（normalize → `+`）。右邊必須緊接**參考字**或**粵拼錨**，唔可接 0243 碼。禁**相鄰**多個（用 `?` 表通配槽）。句首 `+{字面}…` 為頭格連接，唔多一音節；`X…` 與 `+X…` normalize 等價。Legacy `&`／`·` 碼尾仍映射 `+`。
**通配後可選連接**：`?`（或等價通配）**緊接** `+` 再接錨時，`+` 可省略且語意相同（`?+就=`≡`?就=`、`?+^就`≡`?^就`、`?+hon`≡`?hon` 等；舊 `?+=就`≡`?=就` 仍相容）；雙端須一致。無前置通配時 `+` 仍負責詞長分軌（`23o`≠`23+o`）。單格韻／聲錨替代仍係無通配形（`就=`／`^就`）。
_Avoid_：把 **查詢分頁** 開關掣 `+` 當查詢運算符、用 `++` 代替 `?`、`+` 後接碼（如 `2+好+3`）、星號錨（舊稱）；Portable 把 `?就=`／`?^就`（或舊 `?=就`）normalize 成單格而 PWA 留兩格（parity 禁止）

**查詢分派優先序**：
領域規則；**缺字型查詢執行**唔得重排。概略順序：
1. **近反義關係查詢**（`~` `!` `~~` `!!` 連接詞複合…）
2. slot 連接符／語法 hint（連續 `+`／`++`、`+` 後接碼…）
3. 前綴通配等號 hint（`?{≥2字}` 漏尾 `=`）、純漢字多錨 hint
4. **前綴通配等號查詢**（`?香港=`）
5. **串列韻／聲錨查詢**（見 ADR-0052／ADR-0062；單錨**碼夾等號**與 `2^我3`／舊 `2=我3` 唔入串列）
6. **整詞等號查詢**、**碼夾等號查詢**（`{碼}{字}` normalize → `{碼}{字}=`；內嵌聲規範 `2^我3`）
7. **缺字家族**（normalize `+` → **加號錨**、**韻／聲錨**、**通配碼錨**、**粵拼錨**…）
8. **粵拼查詢**、純字面 **詞條 lookup**
_Avoid_：在 executor 重排、把分派細節寫成實作 spec

### 查詢語法家族

**缺字型查詢**：以 0243 碼、**通配符**或漢字描述字面形狀。結果套用**缺字查詢字面優先**，再**搜尋結果排序**。
_Avoid_：mask 查詢、把位置錨定當實作術語

**錨點查詢**：用參考字或粵拼片段錨定特定音節聲／韻／字面（韻／聲錨、加號錨、粵拼錨、串列等）。**位置錨定**易混。
_Avoid_：星號錨（舊稱）

**複合查詢**：~~／!! 等近反義複合，或連接詞複合。候選來自 curated + 素掃描。
_Avoid_：把一般雙字詞當複合

**同音異讀查詢**：左碼/右碼 找同一字面多讀音。結果**按字面合併** + 標註左／右。字面群組依**詞條排序信號**（頻率優先），同字面內純粵拼 lexical。結果清單「N個讀音」徽章：查詢字串含 `/` 且該字面多於一讀時顯示（含 `??/??`）；**PWA** 跟**輸入框即時字串**；Portable 跟已送出／tab 查詢。其他查詢唔顯示（多讀詳情仍在**詞條面板**）。
_Avoid_：左右不等長、單一讀音重複、非含 `/` 結果列顯示讀音數徽章、PWA 只跟已 debounce 送出字串閘徽章

**粵拼查詢**：整段粵拼查詞。
_Avoid_：fragment

**詞條 lookup**：純漢字精確查詢，回傳讀音與**詞條 lookup 版面**。庫無該字面時，經**收錄決策**（詞級標音 raw 或**音節拼接讀音**）產生**記憶體合成列**以編排版面與同碼／同韻鄰詞；**請求路徑唔寫** **詞條庫**（權威收錄仍靠建庫／**詞級標音 curated**）。單字缺 rime／無法拼接則「搵唔到」。
_Avoid_：WordLookup、缺庫就 runtime INSERT、把記憶體合成當已收錄 SSOT

**平仄串列查詢**：
逐音節聲調類別串列：每格為 **平**（`P`）、**仄**（`Z`）或 **同音碼位**（**394052 碼**字母表內數字，並依當前 **平仄** 子檔 `pzmode` 鬆檔）；在 **搜尋模式家族** 嘅 **平仄** 下輸入。規格由平仄 module 嘅 `to_match_spec` 產出（純串列、韻錨、或 mask／缺字 base 上疊 P/Z）。v1 只收 `P`／`Z`／數字／`?`；與其他錨／缺字混寫經 base overlay。
_Avoid_：用「平仄模式」稱呼本查詢類型、PZ 模式、把平仄串列當缺字型查詢、把 **搜尋模式家族** 嘅 **平仄** 當成只有串列一種語法、恢復舊全域自動轉接至 02493／394052

**平**（平仄）：
**平仄串列查詢**中，該音節 **0243 碼** 為 `0` 或 `3` 之聲調類別；輸入字母 `P`（大小寫等價）。
_Avoid_：陰平、陽平（作查詢語法正名）、把粵拼調值 1／2 直接當平

**仄**（平仄）：
**平仄串列查詢**中，該音節 **0243 碼** 唔係 `0` 或 `3`（即 `2`、`4`、`9`）之聲調類別；輸入字母 `Z`（大小寫等價）。將來更細聲調矩陣（如三聲與五聲分開）若仍歸入仄類，**平仄串列**聚合結果不變。
_Avoid_：仄聲（泛稱）、把入聲 `9` 當平

**同音碼位**（平仄串列）：
**平仄串列查詢**中，以數字 `0`–`9` 標示該音節須與該聲調數字**同音**（先 **02493 碼** 正規化再對詞庫 **0243 碼** 逐位比，精確比對）。
_Avoid_：碼字、把數字格當字面

---
## 詞庫與排序

**詞庫**：
決定哪些字面是「真詞」、以及詞級讀音（字面＋粵拼＋**394052 碼**）的權威來源。搜尋用的詞條庫是詞庫的副本；注入須經**收錄決策**（單字 rime、多字詞級標音、或音節拼接讀音），禁止憑空造字或繞過收錄決策猜音。可感知變更須透過 **詞庫版本 bump** 或 **詞庫刷新**（更新 manifest 完整性）令離線客戶端重新拉取；唔雙讀舊 **0243 碼**存儲。
_Avoid_：把自動猜讀當詞庫、測試用造字進正式收錄、靜默換庫而不更新 manifest 完整性

**詞庫版本 bump**：
改變 **詞庫** 發佈標籤（manifest `lexiconVersion`，如 `v1.0.7`）；OPFS／快取鍵跟此標籤。發佈檔名可為固定 **`lyrics.db`**（現行）或歷史 `lyrics.{tag}.db`。用於格式遷移或與 app semver 對齊嘅可感知斷點。
_Avoid_：把 app tag 同詞庫標籤混為一談（兩者可以獨立）、只改檔名唔改 manifest 版本

**詞庫刷新**：
維持同一 **詞庫版本** 標籤，替換詞條庫字節（如同一 tag 內容修正）；須更新 manifest `byteSize`／`sha256`，令已快取客戶端淘汰舊副本並重拉。
_Avoid_：刷新而不跑發佈閘、刷新而不做煙霧驗收

**詞庫掛載**：
將 SSOT **詞條庫**發佈物同步到各交付渠道可讀位置（含完整性 metadata），令 **PWA 交付頻道** 與 **免安裝交付** 唔各自維護漂移副本；runtime 仍可分渠開庫。
_Avoid_：mount（作領域正名）、把掛載當單一 live 共用連線、把只下載唔校驗當掛載完成

**詞庫渠道同步**：
**詞條庫建置流程** 成功且 **詞庫發佈閘** 通過後，將根目錄 SSOT `lyrics.db` **複製**（唔 hardlink）到 PWA public 樹並刷新 `lexicon-manifest`（含可選 gzip）；閘紅唔自動同步。
_Avoid_：把 hardlink 當預設、閘失敗仍當 ship 就緒、略過 manifest 完整性

**詞庫開發掛載**：
本機 dev 優先讀 repo 根 SSOT **詞條庫**字節（中介層 stream），唔依賴過期 public 副本；**唔**取代 **詞庫渠道同步** 產物（build／Pages 仍用同步後 public）。
_Avoid_：只靠人手 copy 先可 dev、dev 同 prod 混為同一掛載路徑

**詞庫埠**：
詞庫資料的抽象邊界：**raw lookup**（rime 單字表、詞級標音 JSON，不帶收錄判斷）與**排序信號**（**Essay 詞頻**、**排序用 curated**、pron_rank）。**收錄門檻**（能否注入、讀音歸哪一類）由**收錄決策**單獨判定，不由埠的 lookup 方法隱含決定。
_Avoid_：把 runtime 詞條庫本身稱作詞庫、把收錄決策塞進 raw lookup 回傳值

**詞庫快取索引**：
詞條庫在 runtime 記憶體中的可查索引與就緒狀態；就緒後供搜尋路徑依字面形狀與聲韻維度取向量候選，未就緒時搜尋降級直查詞條庫。**離線啟動預載**的編排與磁碟暖啟屬餵入此索引的實作細節，不與索引介面混稱。**免安裝交付**的 Portable 套件可內含與捆綁 `lyrics.db` 對應的預暖磁碟快照，縮短首次**就緒閘**等待。
_Avoid_：word_cache（作領域正名）、把預載執行緒或磁碟快照當成索引定義的一部分

**詞級標音家族**：
多字收錄（詞級標音優先，否則拼接）、詞級異讀（上游決定）、讀音合併（`(字面, 粵拼)` 唯一；同層可多讀；**跨層限制**＝低源可**新增**尚未出現嘅粵拼，**唔准改寫**高源已有嗰對，只合併**詞條來源紀錄**）、格式門檻（合法即收）、上游優先序（勘誤 > words.hk > 開放 > curated；同讀時高源碼作準）、**音節拼接讀音**（fallback，非詞級）、單字收錄（rime 預設）。
_Avoid_：混稱拼接為詞級、認領後鎖死成個字面嘅讀音集、rime 過濾異讀、essay 單獨收錄

**音節拼接讀音**：
庫無該多字字面嘅詞級標音時，以各字 **rime 預設**（**pron_rank**）單字讀音串接而成嘅 fallback；用於 **詞條 lookup** 記憶體合成列。揀音跟 pron_rank（預設優先；有合格候選時剔罕見／棄用），**唔**用 essay 詞頻或詞條庫插入順序；**唔**寫入 **詞條庫**。
_Avoid_：當詞級標音、用 DB 第一列／最高頻當預設、請求路徑 INSERT 合成列

**分類詞語來源**：
`rime-cantonese-upstream` 以類別 CSV 提供嘅低權威 **詞條 SSOT 來源**；預設只收 `word`、`fixed_expressions`、`phrase_fragment`、`onomatopoeia`、`trending`，入口明確排除 `proper_nouns`。保留上游詞級粵拼，沿用 `rime_words` source id、排序及跨層限制；三／四字詞按詞語類別收錄，唔按長度通殺。
_Avoid_：經下游合併 YAML 重新混入 proper nouns、用 Essay 詞頻做收錄門檻、另開 source flag

**短語補缺來源**：
舊 `jyut6ping3.phrase.dict.yaml` 嘅非預設、維護者顯式分析來源（`rime_phrase`）；只用於找出其他可信來源未覆蓋而值得移入 curated 嘅少量固定短語。歷史檔混有輸入法用戶詞庫及城市資訊，唔再參與預設 **詞條庫建置流程**。
_Avoid_：把 phrase raw 全量或過濾後餘量當預設詞庫、直接靠自動讀音收錄、用拒收表長期清洗黃頁型來源

**短語收錄門檻**：
維護者顯式分析 **短語補缺來源** 時判定字面可否成為 rescue 候選嘅相容規則集；拒收者只計統計。規則唔再承擔預設詞庫除噪責任，真正收錄須移入可信分類或 curated。
_Avoid_：filter（作領域正名）、重新啟用 legacy phrase 作預設、把拒收字面表當第二套詞庫

**拒收字面表**：
維護者宣告嘅整詞拒收清單，屬 legacy **短語收錄門檻**；只約束顯式分析 **短語補缺來源**（`rime_phrase`），字面完全相等先拒。
_Avoid_：blocklist（作領域正名）、套用到高權威來源、用後綴規則代替整詞表

**詞條庫瘦身**：
維護者對正式 **詞條庫** 發佈物嘅體積與內容質量優化。**主指標**為解壓後詞條庫字節；**輔指標**為搜尋噪音（無意義字面命中率）。傳輸體積與冷啟動時間須以基準量度與煙霧驗收約束，唔可以單靠瘦身犧牲就緒體感。
_Avoid_：把 gzip 當唯一瘦身指標、把刪詞當唯一手段、未量度就改分包形狀

**詞庫發佈閘**：
**詞條庫建置流程** 完成後、**詞庫刷新** 出包前嘅自動量度門檻。**I2**：解壓後詞條庫 ≤95MB、索引總體 ≤45MB、legacy **短語補缺來源**預設收錄為 0（兼容閘仍以舊基線驗下降）、**黃金查詢集** 100% parity、G benchmark 閘前 p95 唔差於基線 +10%。**I3（J2）**：在 I2 基線上再要求 ≤75MB／索引 ≤35MB，等號查詢 p95 唔差於 I2 基線 +5%。
_Avoid_：gate（作領域正名）、閘只量檔案唔跑查詢、未過閘仍 ship

**詞庫分包**：
將詞條庫拆成可獨立下載／開庫嘅多個 SQLite 發佈物，以縮短 **就緒閘解鎖** 必載體積；未載入嘅分包由 **啟動完畢** tail 補齊，查詢依既有 fallback 降級。**v1.0.7 唔做**；改以 **詞條索引瘦身** 為主。
_Avoid_：shard（作領域正名）、把分包當刪除關係資料、閘前仍下載全量合併檔

**詞條索引瘦身**：
維護者縮減 **詞條庫** 內 SQLite 二級索引體積（常佔解壓後檔案大部分），以 **EXPLAIN QUERY PLAN** 與黃金查詢驗證後先刪；優先剔除重複索引同未被規劃器選用嘅單欄索引，再配合 **分類詞語來源**及退役 legacy phrase 減少索引行數。**v1.0.7** 目標 **I2**（刪重複 + 審計刪單欄索引），須 **TDD** 先寫失敗測試再改 ingest／schema。
_Avoid_：index（作領域正名）、未驗證就刪複合索引、把刪索引當唯一瘦身手段

**詞條庫字數不變式**：
**詞條庫** 每列 `words.length` 必須存在、非零，並等於字面 Unicode code point 數。正式發佈物由**詞庫發佈閘**與雙端開庫驗證；違反者拒絕開庫，PWA 清除該版本快取再下載。本機 legacy 詞條庫只經顯式 repair adapter 以單一 transaction 修復，完成後先通過**就緒閘解鎖**。正常查詢只用 `length = ?` 命中複合索引，唔再自行計 `length(char)` 兼容。
_Avoid_：在每個查詢 caller 保留 `NULL/0` fallback、背景回填期間先解鎖搜尋、用 `wordMatchesWidth` 掩蓋壞庫、為此重建整張表加入 `CHECK`

**音素欄位緊湊化**：
將 **詞條** 上聲母／韻母欄由冗長字串改為**字典序短編碼**（固定音素表、定界串），複合索引鍵跟緊湊格式（**詞條索引瘦身 I3／J2**）。查詢比對仍用聲母／韻母 token 語意；舊庫形須遷移或從源重建。閘硬 cap 以 **詞庫發佈閘** I3 為準（目標可按量度修訂）。
_Avoid_：phoneme id（作領域正名）、把編碼形當第二套讀音語意、未遷移舊庫硬開

**真緊湊（存儲）**：
令**權威載體上可觀測位元組**（表頁＋必備索引，或等價 blob／分包檔）相對冗餘正規形下降**一個數量級級**，且語意／查詢契約不變；只靠 gzip、或只縮短字串欄而百萬行＋雙向 B-tree 仍在，**唔**算真緊湊。量度以 VACUUM 後 `dbstat`／檔案大小＋gzip 對照舊渠道包為準。
_Avoid_：只 gzip 當完成、把「少寫幾個 source 字元」當治本、犧牲正確性截斷池當緊湊

**語意鄰居緊湊載體（草案）**：
**語意向量鄰居烘焙** 之 A 產物唔長期以每邊一列 `word_relations` 承載；改以每 head（或 CSR）**緊湊編碼**鄰居 id＋量化 score（版本指紋，如 `embedding_nbr_version`），**近反義池投影** decode 後併入 `semantic_related`（source 仍可標 `embedding_cosine` 排序）。與 **音素欄位緊湊化** 同紀律：存儲形≠語意形、雙端／開庫契約、舊形 migrate 或 rebuild。細節見 `docs/working-plans/2026-08-05-embedding-neighbor-compact.md`。
_Avoid_：把 110 萬語意邊當永久 edge 表方案、runtime 載向量模型、cosine 當 ant

**I2 雙端穩定**：
**詞庫發佈閘**（I2）全過，且 **PWA 交付頻道** 與 **免安裝交付** 在相同重建詞條庫上通過 **S-B 煙霧**（自動閘 + PWA／Portable 核心查詢）與黃金查詢 parity、無已知阻斷缺陷；**維護者手動測試**反饋確認後，方可啟動 **音素欄位緊湊化** PR。
_Avoid_：只驗一端、只跑單元測試唔做離線就緒、把「合併咗 PR」當穩定、跳過維護者手動驗收

**詞庫核心包**：
**詞庫分包** 中 **就緒閘解鎖** 必開嘅分包，承載 **詞條** 主表與 0243 搜尋所需索引；唔含 `word_relations`。
_Avoid_：core db（作領域正名）、把核心包當完整詞條庫

**詞庫關係包**：
**詞庫分包** 中承載 `word_relations` 嘅延後分包；**近反義模式** DB 關係查詢於載入前依 **靜態詞林埠** 等 fallback；載入後恢復完整 **近反義池投影**。
_Avoid_：relations shard（作領域正名）、把關係包當可選刪除

**詞條庫建置流程**：
建置命令（單一入口，≤10min，bulk relations <10s）、從源重建（全量 wipe+ingest+overlay）、SSOT來源（rime+words.hk+開放+curated）、源清單（yaml enabled）、fixture（CI 用）、CC-Canto 脫離。允許 **源層內容定址 cache**（只加速 parse；merge／overlay 每次重跑；寫入仍 wipe＋全量 persist）。
_Avoid_：legacy 當 SSOT、增量 patch（指跳過 wipe／直接改現行 **詞條庫**）、完整 db 版控、把**戰役工具**當本流程一步、把源層 cache 叫成增量 patch

**戰役工具**：
維護者 one-shot（campaign freeze／audit／batch apply 等）；**唔**屬 **詞條庫建置命令** 執行路徑。產出可入 git 清單或 **專案自建詞性清單**，再由建置／發佈面讀取。清單 collect 同 **獨立詞性載體** 之 `project_pos build` 仍屬發佈／建置面，唔算本詞。
_Avoid_：把戰役腳本當 rebuild 一部分、同 **詞條庫建置流程** 混樹、稱作 ingest 子命令（作領域正名）

**Essay 語料**：
粵語書面／口語詞頻語料（[rime-cantonese](https://github.com/rime/rime-cantonese) · [essay-cantonese.txt](https://github.com/rime/rime-cantonese/blob/main/essay-cantonese.txt)，CC BY 4.0），格式為「詞＋詞頻」。**不**開注入門、**不**用於猜讀音；僅供排序。語料隨 repo 提供。
_Avoid_：把語料當詞庫、把語料當近義來源

**Essay 詞頻索引**：
啟動時自 Essay 語料建立的**字面→詞頻**查表，僅供搜尋排序；載入記憶體，不寫入詞條庫、不另建詞頻表。更新語料檔後重啟即生效。
_Avoid_：詞頻表（作資料庫實體）、把詞頻存進詞條列、用 vector embedding 取代詞頻排序

**排序用 curated**：
維護者維護嘅**常用字面**集合（`data/lexicon/curated_common.txt`），只提供搜尋結果排序嘅布林加分（契約中 `-curated` 一截）；**唔**負責收錄／標音。與 **詞級標音 curated** 分家：標音收錄**唔**自動寫入本集合；要碼搜靠前須人手另加。預設唔加（含為反義而收之字面）。
_Avoid_：把本檔同 `curated_lexicon.json` 混一、收錄即自動加分、用本檔當詞庫 SSOT

**排序信號**：
essay／**排序用 curated** + pron + lexical。多契約 tier：
搜尋結果：han → -essay → -curated → pron → 字面
等號參考：pron → -essay → !aa → jyut
同音異讀：han → -essay → -curated → ch/jyut（內 lexical）
**0243／碼類扁平搜尋**下，Essay＝0 且唔在 **排序用 curated** 嘅新收字沉底係**預期**（常用優先）；發現冷字靠純漢字 lookup、近反義／`!`、或之後人手加 **排序用 curated**——唔另開「新收」結果分區，亦唔為此改近反義池排序契約。
_Avoid_：把契約收成一條 tier、essay_sort（實作名）、把標音 curated 當 -curated 來源、把碼搜沉底當缺陷而自動抬頻

**等號參考讀音選列**：
多讀音選權威用**排序信號**等號契約。_Avoid_：與搜尋共用 tier、規則散落

**詞條 lookup 版面**：
純漢字查詢回傳多段：code/粵拼 header → 查詢詞 → 同碼同韻段（含字面→其他→異碼）→尾韻錨→餘下。各段用**排序信號**。缺庫時以**詞條 lookup** 嘅記憶體合成列作 exactMatches 編排（同上）。
**Portable**/**PWA** 共用順序；Portable 另標題列，PWA 內嵌。_Avoid_：code_aware、隱式 sync、刪 Portable 標題為對齊 PWA

**缺字查詢字面優先**：
**缺字查詢**結果先比參考字吻合數，再**排序信號**。_Avoid_：mask_priority、吻合與頻率混

**搜尋結果排序**：
扁平清單用**排序信號**搜尋契約。**缺字查詢字面優先**前置。異讀/等號變體。收錄入庫 ≠ 抬升本排序。
_Avoid_：預設字母、每語法一套、混粵拼純漢、完整快照 CI、為新收字另開排序契約或 UI 分段

**擷取頁**：
後端或**瀏覽器查詢引擎**一次回傳嘅結果列（可為同一字面多讀音列）；**擷取頁上限**現行 800 列（搜尋列 load-more／續頁用此上限）。**0243搜尋模式**家族（**0243模式**／**02493模式**／**394052模式**）**首查**用較細嘅**首屏擷取**（現行 400 列），以縮短連續查詢 wall time；近反義池沿用既有較細頁。**句格工作台**候選亦用同一首屏 400；續載每批 +400，**無**全域硬頂（單次請求上限仍 400）。擷取列數係引擎續頁單位，**唔**等於 UI「結果數」。
_Avoid_：page size（作領域正名）、把擷取列數當「N 個結果」、把首屏擷取當上限、首查強行撈滿上限、工作台另立與搜尋矛盾嘅 120 硬頂

**結果數**：
標準詞條列表統計「搜到 Y 個結果」＝引擎回傳嘅**合併後可點字面**總數（同清單掣一一對應；分批擷取／呈現唔改變 Y）。未有字面總數前唔顯示結果數文案。近反義／粵拼錨分欄用各自計法。**句格工作台**「池內 M」＝ MatchSpec 排序池總列數（詞性篩選前）；「已載／篩後」為目前畫面累加可見數。適用 **PWA** 與 **Portable**。
_Avoid_：用讀音擷取列數報「個結果」、把「已載入 N」當標準列表主文案、同音異讀另開一套統計單位、把工作台分組小計當池內總數

**呈現批次**：
**結果捲動區**內每次捲動觸發多畫幾多個合併後詞條（現行 400）；只擴已擷取範圍內嘅列，唔自動連環拉完整個總數。
_Avoid_：infinite scroll（作領域正名）、一次畫晒擷取頁、未捲動就自動擴批次

**結果捲動區**：
搜尋表單以下、獨立垂直捲動嘅結果列表容器；捲動條只控制此區，唔帶動整頁。
_Avoid_：整頁捲動當結果捲動、用 padding 假裝有捲動

**搜尋教學驗收**：
`shared/guide-i18n.mjs` 內每個可執行範例，喺 **Portable**（後端 `search_words`）同 **PWA**（**瀏覽器查詢引擎**）對同一 **詞條庫** 皆須回傳 ≥1 列；零結果視為回歸。兩端範例清單以 i18n manifest 為準，HTML 按鈕須與之同步。
_Avoid_：只驗一端、用合併後 DOM 詞條數當探針、教學例子各寫各唔對齊

**探針暖機**：
教學回歸探針斷言前須達 **就緒閘解鎖** 同等引擎狀態（唔等 **啟動完畢** tail）。Portable：`warm_guide_probe_readiness`（**詞庫快取索引** + `事業` 探針）；PWA Node：`warmGuideProbeReadiness`（閘前輔助索引 + `事業` 探針）。兩端共用契約。
_Avoid_：冷索引直接 assert、探針繞過 gate 卻唔暖機、把 tail 靜態詞林埠當教學探針前置

**教學探針 CI 閘**：
固定 **詞條庫** + **近反義池快照** + **探針暖機**；**搜尋教學驗收** manifest 每例在 Portable 與 PWA 都須回傳 ≥1 列。另設小型 **relation runtime probe**，由原始關係資料重建／刷新快照，覆蓋近義、反義、連接詞複合、fallback 與失效。CI 預設層。
_Avoid_：只驗精選例子、每例重建 runtime、用冷快照、省略 runtime probe

**教學探針全量閘**：
repo 解壓後 **`lyrics.db`** 上以完整 relation runtime 對 manifest 每例雙端探針（Portable + PWA）；維護者與發佈前必綠。
_Avoid_：只跑 CI 閘當 parity 完成、雙端用唔同詞庫卻當通過、把快照驗收當完整 runtime 驗收

**教學 parity 切片**：
PWA 對齊 **瀏覽器查詢引擎** 與後端時，按搜尋教學語法族逐 PR 修復（粵拼 lookup → mask／prefix → 加號錨 → 粵拼錨 → 關係查詢）；每片紅→綠後併入全量閘計數。
_Avoid_：單 PR 混多族、未綠就宣稱 parity 完成

---

## 產品邊界

**離線單機交付**：創作者本機完整查韻/搜尋/近反義；詞庫+快取本地。_Avoid_：自架服務並重

**免安裝交付**：創作者**唔使**預裝 Python／pip／uv；Win/Mac 雙擊啟動本機產品並內建詞條庫。**首次啟動可需網路**建立執行環境；**其後離線**即可完整查韻。正式 **Desktop 套件**唔以內建 wheels／離線 bootstrap 物料為預設；無網首次須失敗得清楚。Linux 不承諾雙擊免安裝。
_Avoid_：要求創作者安裝 Python、把「首次零網路」當免安裝必要條件、只在建置機驗證啟動即當交付完成、把可選離線大包當現行主渠道

**Desktop 套件**（正名；舊稱 **Portable 套件**）：zip/tar 等 **免安裝交付** 發佈物；含平台 launcher（Windows **只** `.exe` 入口，**唔**附 `.bat`）、**可安裝應用包**、以及**側車**詞條庫與本機產品 UI。正式套件帶 **套件發佈指紋**。產品 UI 入口為 **`/app/`**。執行環境由 launcher 在使用者機器管理，**唔**再以「整包 venv 跟資料夾任意搬移」為產品承諾。查詢以側車根 **`lyrics.db`** ＋本機服務為準；**產品 UI 側車**與 **PWA 靜態束** 分離——Desktop 組裝**唔**內嵌 PWA 瀏覽器引擎用嘅第二份庫／static 近義等索引（見 [ADR-0068](docs/adr/0068-desktop-pyapp-delivery.md) §13）。
_Avoid_：Portable 套件（作產品正名）、把 Desktop 同 **PWA 交付頻道** 混稱、要求創作者自行打包 runtime、Windows 正式套件附 START.bat、把詞庫只藏在執行環境安裝樹當唯一來源、為「體積對齊」把 PWA 死重塞入 mac／Windows Desktop

**venv 運送包**（過渡／歷史）：舊 Portable 建置將可搬移 `venv/` 收成少數大檔、首次啟動 extract-once 的運送形態。**Desktop 正式渠道淘汰**；可短暫留 legacy 建置開關做對照，**唔**再上正式 Release。
_Avoid_：把運送包當現行 Desktop 契約、當第二套查詢引擎、正式渠道雙軌長駐 venv 包與 Desktop 包

**套件發佈指紋**：
某次**正式** **Desktop 套件** 發佈嘅身份：`release tag` ＋ **本平台套件 digest** ＋ **詞條庫**（`lyrics.db`）sha256；三者須同一次發佈寫入套件。用以判別本機套件是否仍對應最新正式遠端發佈（含同 tag 換庫或程式刷新）。
_Avoid_：git 版本、只 semver／只 **詞庫版本**、update check id、把 prerelease／beta 當正式指紋來源

**套件更新提示**：
本機 **套件發佈指紋** 與最新**正式**遠端指紋唔一致時，向 **創作者** 顯示嘅通知（啟動終端摘要 ＋ 產品 UI 橫幅）；引導前往 GitHub Release 或複製下載指令，經關閉程式後人手解壓覆蓋成個 **Desktop 套件**；**唔**原地自動覆蓋、**唔**阻 **就緒閘解鎖**。只對非 prerelease 嘅最新正式 Release；遠端 beta／prerelease 唔觸發。無本機指紋則唔檢查、唔提示。短超時失敗當無更新（fail-open）。**現行**唔啟用 launcher 自動更新；整包自動同步屬長遠目標，唔改變而家提示＋人手覆蓋契約。
_Avoid_：強制升級閘、系統 MessageBox（v1）、把 git pull 當 Desktop 更新路徑、穩定軌道提示 beta、把長遠自動更新當已交付能力

**本機服務退出**（Desktop）：
本機查詢服務之停止。**預設**：關閉**最後一個**本機產品瀏覽器分頁時停止服務（產品內 **查詢分頁** 已承擔多工作區，唔依賴多開網頁 tab）；**刷新／軟導航唔停服**。進階可改為「關分頁唔停服」並顯示選單內「停止本機服務」。Launcher 可喺啟動後結束；服務常駐至上述停服條件或進程被系統結束。
_Avoid_：把 F5／reload 當停服、假設多開網頁 tab 先做得到多工作區、假設存在擁有後端生命週期嘅原生 App 窗（無另做殼時）

**Desktop macOS 啟動**：
創作者主路徑為 **`Canto-0243.app`**（薄 **Desktop 安裝進度殼** + 包內 PyApp runtime）。**側車**（`lyrics.db`、產品 UI、wheel 等）與 `.app` **同層**，解壓資料夾整體搬移；**唔**只拖走 `.app`。正式套件**唔**再附 **`.command`**。Finder／Dock 圖標用產品 **PWA 主圖**（`icon-512` 等）生成之 App 圖標。Gatekeeper：**只應對 `.app` 做一次**「右鍵打開／仍要開啟」；殼在 spawn 內層 runtime 前清除 payload 與 bundle 內 quarantine，**唔**要求創作者對 runtime 再 bypass。**未 notarize** 仍可能有第一次提示；**唔**承諾雙擊零提示。
_Avoid_：教 `.command` 作主入口、要求對 `runtime/` 內執行檔再「仍要開啟」、只拖 `.app` 入 Applications 而留低側車、把 ad-hoc／未 notarize 當已解決所有 Gatekeeper 提示

**Desktop 安裝進度殼**（首次）：
雙擊 Desktop 入口後、產品 UI／**就緒閘**出現前，由**薄原生外層殼**顯示嘅品牌化前置進度界面。**一份靜態 HTML splash 資產 + 跨平台宿主源碼**（Rust + wry／tao；Win WebView2、Mac WKWebView），編譯兩個平台入口；**唔**跑產品 Vite／React。殼包住／先於內層 PyApp：覆蓋下載 CPython、建 env、安裝應用包等；完成後交棒產品。已就緒安裝**跳過殼**。進度以**分階標籤**＋**階段映射估計 %**（文案「約 N%」／en `~N%`；定長 bar 跟同一 N；% 只隨階段跳升、單調不降、階段內唔時間爬）為主；**唔**宣稱位元組級精確。**唔**等同 **就緒閘**／**閘前進度**。
_Avoid_：把安裝進度殼叫就緒閘、要求 React 閘在無 Python 時渲染、單一 exe 跑晒兩平台無抽象、fork 改 PyApp 當唯一進度 UI、位元組假精確 %、純時間線性假爬 %

**指紋略過**：
創作者對某一個**遠端套件發佈指紋**選擇稍後／關閉；同一指紋唔再出現 **套件更新提示**，遠端指紋一變則恢復提示。
_Avoid_：固定 N 日 snooze、永久關閉更新檢查（作預設）、略過「成個 tag」而唔理同 tag 指紋變更

**Canto-0243 License**：整包 CC BY-NC-SA 4.0 + 附加；署名見 THIRD_PARTY_NOTICES。

**靜態客戶端束**：PWA 核心產物；client/ Vite 打包至 dist/；含**瀏覽器查詢引擎**、sql.js／OPFS 開庫物料、**詞庫**（plain 與可選 gzip）、閘前／近反義用靜態 index（ranking／static-syn 等）。部署 GitHub Pages。產物體積**遠大於** **Desktop 套件**內 strip 後 UI（Desktop 查詢走本機服務＋根側車庫，唔塞 browser 庫／static-syn）。**唔**為對齊 Desktop 體積而刪 PWA 庫或靜態 index（會傷就緒閘／查詢路徑）。
_Avoid_：以為 dist MB ≈ Desktop zip MB、為瘦產物刪 static-syn／只留單庫檔而無相容策略

**瀏覽器查詢引擎**：TS 對口 py 引擎；0243 解析/比對/排序/近反義；直操 lyrics.db 給同等體驗。

**PWA 交付頻道**：靜態束 + SW（Cache First 離線）+ manifest；支援「加到主畫面」；GitHub Pages 主站。首訪 wire 以 **詞庫壓縮傳輸**（prefer gzip）與按需／tail index 為主，**唔**等於一次下載成個 dist 碟上總和。

**詞庫預取**：
Service Worker 於 `install`／`activate` 以低優先級預拉當前版本詞庫包入 CacheFirst；不阻塞**就緒閘**，僅令後續開頁有機會走**詞庫暖啟動**。**省流量**或疑似 metered 網路時跳過自動預取。預取 URL 與**詞庫壓縮傳輸**一致（優先壓縮檔）。
_Avoid_: prefetch（作領域正名）、把預取當**就緒閘解鎖**條件

**詞庫壓縮傳輸**：
發佈側產 gzip 詞庫包（`lyrics.{version}.db.gz`）；客戶端顯式下載並串流解壓寫入 OPFS，唔依賴 CDN 透明壓縮。manifest 標示是否優先壓縮檔（建置時節省未達門檻則仍用 plain `.db`）；完整性 `byteSize`／`sha256` 一律指解壓後 SQLite。**閘前進度**文案不因壓縮而改變。
_Avoid_: brotli（作本渠道預設）、整包解壓再寫入（易拖慢載入）、把壓縮檔 sha256 當詞庫完整性

**瀏覽器分頁使用**：經一般瀏覽器分頁開啟同一交付頻道站點；未「加到主畫面」仍屬該頻道，唔等同關閉交付或停用離線就緒。
_Avoid_：把「未安裝 PWA」說成關掉 PWA 交付頻道、未安裝即每次重開都要重新下載詞庫

**詞庫暖啟動**：本機已有該版本詞庫副本時，開頁即從本機開庫（不經網路），唔重新下載。快徑：開庫與驗證喺短閾值內完成則可跳過就緒閘 overlay；否則用簡化**閘前進度**（無 landing 儀式）。完成後可直接搜尋。
_Avoid_：暖啟動、warm start（作領域正名）、把暖啟動說成重新下載詞庫、詞庫暖啟動仍強制完整 landing

**詞庫開庫後端**：PWA 開啟詞庫嘅執行路徑；**預設**用 OPFS 直開 SQLite（`opfs-vfs`），唔可用時**開庫降級**至整檔 sql.js。維護者可用 build env 強制 `sqljs` 做對照，產品語境只講預設與降級。
_Avoid_：backend、VITE_DB_BACKEND（作領域正名）、把開庫降級說成重新發佈詞庫

**開庫降級**：`opfs-vfs` 開庫失敗時，同一 init 內改走 sql.js；仍走 OPFS→SW→網路復原順序，唔額外發起第二輪懶觸發。
_Avoid_：fallback backend、自動換 pipeline

**靜態詞庫帶寬風險**：公開 `lyrics*.db` URL 可被直接反覆下載；client 緩存與 session 黏性只減正常重訪與同 session 濫用，**擋唔住**跨無痕／腳本直打靜態檔；邊緣限速屬交付跟進，唔算開庫降級範圍。
_Avoid_：話 PWA 快取可防 DDoS、把帶寬問題當開庫 bug

**開庫後端標示**：頂欄按**實際開庫後端**顯示「· OPFS」；**開庫降級**至 sql.js 時唔顯示，避免誤導。
_Avoid_：用 build env 標示後端、降級時仍顯示 OPFS

**就緒閘**：**就緒閘解鎖**前阻所有**查詢分派**搜尋；傳**閘前進度**。解鎖當刻須露出產品殼（可搜尋／導航）；**唔**再以閘 UI 狀態令殼長時間空白。冷啓首次可短 overlay 淡出；熱啓／已 landed／詞庫暖啟動快徑**零延遲**露殼。
_Avoid_: 解鎖後仍用閘 open 狀態擋殼 1s+、把 landing 儀式當阻殼條件

**閘前進度**：
**就緒閘解鎖**前向創作者傳達的載入進度。**首次須下載詞庫**時須含下載 byte 比例與分階標籤（下載／開庫／驗證）；**詞庫暖啟動**可走簡化路徑（省略或縮短進度與 landing 儀式）。與**背景預載標示**（tail）分開。Portable 閘前唔再綁 **詞庫快取索引** 建表進度。單次閘前 init 內進度**只進唔退**（開庫降級或階段重報唔令墨水縮返）；新一輪 init／重試先清高水位。
_Avoid_: progress（作領域正名）、把閘前進度當 tail 預載進度

**搜尋教學**：
創作者面向嘅完整教學殼（與 **搜尋教學速覽**、**查詢語意解釋** 組成同一套**創作者教學文案**）。產品入口仍為單一「搜尋教學」；殼內兩個子頁：**搜尋指南**（查詢語法，即原完整教學正文）與 **押韻指南**（**韻母比對檔** 四檔點用）。標題、intro、範例標籤用填詞人聽得明嘅說法；**運算符符號**（`=`／`?`／`+`／`~`／`!` 等）照舊教，唔用中文口令取代。技術正名（串列、通配、錨、遮罩、碼夾等號等）只留本詞彙表／ADR，唔作教學標題。創作者已慣用之詞（同韻、同聲、平仄、近義、反義）可保留。**搜尋指南**章節順序以**填詞常用度**為先；單格同韻／同聲與整詞／數字夾字合為同一教學組；完整教學可分「常用／進階」內容組；多重位置同韻／同聲（數字＋參考字、留空、頭字任意）合為同一教學組。完整教學嘅例子說明字與 **查詢語意解釋** 摘要一致。速覽係精選入口，唔要求同完整教學頁面結構一一對齊。
_Avoid_：把技術正名當教學標題、另開第四套文案 SSOT、用教學文案改寫查詢語法行為、用中文口令取代運算符符號、按實作家族名排序而忽略常用度、同韻／同聲拆成多張互不連貫嘅卡、要求速覽與完整頁章節一一對齊、例子說明同語意解釋兩套字、把兩個子頁拆成兩個頂層 menu／tab 類型（第一期）

**搜尋指南**：
**搜尋教學** 子頁：承載原查詢語法教學全文（章節、可點範例、常用／進階組）。領域上唔另開第二套語法 SSOT。開 **搜尋教學** 時預設本子頁；URL 可標子頁（缺省＝搜尋指南）。
_Avoid_：搜尋教學（單指語法正文時應用本詞）、語法教學（作唯一正名）

**押韻指南**：
**搜尋教學** 子頁：教 **韻母比對檔** 四檔（**正韻**／**通韻**／**腹韻**／**尾韻**）點理解、點揀、同搜尋／句格點用；架構可跟參考分組示意，文案用本產品用語（押韻模式／四檔正名），唔照抄外站字眼。四檔**完整分組展示**由 **韻母分組表**（runtime 同源資料）**渲染**，唔喺教學文案手抄第二份韻母表。**通韻**卡名：多韻互通稱「部」（rhyme2 二十轍系，如依時部、郎當部、欄柵部）；單細韻稱「韻」（如雞啼韻、倫敦韻）。**舒入相配**（rhyme.txt）：入聲併入對應舒聲部，卡內列齊舒聲＋歸部入聲，**唔**另開入聲卡；引擎通韻 expand 同一套。每檔可有少量可點小例（開搜尋 tab）；第一期**唔**強制納入 **搜尋教學驗收** 全量探針清單。可經 URL 深鏈；唔作預設首屏。**Desktop** 與 **PWA** 同一子頁契約與文案源；**搜尋教學速覽**唔拆兩頁（可選單點入口跳本子頁）。
_Avoid_：把押韻指南當第二套分組表 SSOT、用「精準押韻／字腹韻」等外站名取代正名、把本頁當可改引擎行為嘅規格、i18n 手維全表韻母 list、第一期就令可點例依賴當前 **韻母比對檔** session 做 CI 硬閘、只做一端教學、速覽亦拆成兩個完整子頁、入聲另卡／另部當通韻預設、教學舒入相配但引擎入聲另混（或相反）

**搜尋教學速覽**：
空查詢搜尋首頁上對 **搜尋教學** 嘅精選運算符摘要；可點範例觸發 **查詢分派**；完整內容仍屬 **搜尋教學**。UI 文案用「教你點用」；用語同屬**創作者教學文案**。完整教學重排／分組時，速覽可保持獨立精選。
_Avoid_：另開第二套語法 SSOT、同 **查詢語意解釋** 混稱、把速覽當完整教學分頁、速覽標題用技術譯名、要求速覽與完整頁章節一一對齊

**就緒閘解鎖**：
就緒閘放行、創作者可送出**查詢分派**搜尋的狀態；同時產品殼須可交互（見 **就緒閘**）。Portable／server 以 **詞條庫可查探針**（預設字面「事業」）為準；PWA 以詞庫可開啟且離線就緒探針查詢成功為準（見 `offline-readiness.md` Ready 定義）。**唔**等同**啟動完畢**，**唔**要求 **詞庫快取索引** 已建完。
_Avoid_: gate_ready（作領域正名）、把解鎖等同詞庫下載完成或 word_cache 建完、PWA 解鎖前跳過探針、解鎖後空屏等儀式完先出殼

**啟動完畢**：
**離線啟動預載** tail 段全部完成、背景無待載入項的狀態。PWA **閘前**已載**韻母字母表**、**複合詞表**、**排序信號** JSON；PWA tail 為**靜態詞林埠** → **詞庫字面集** → **音素倒排**。Portable tail 含 **詞庫快取索引** 建／restore、靜態語料與近／反義複合快照等。**就緒閘解鎖**後至**啟動完畢**前，創作者仍可搜尋；缺 **詞庫快取索引** 時 mask 等走 SQL 降級。
_Avoid_: startup_complete（作領域正名）、啟動完畢（作詞庫下載或 OPFS 落地完成）、把 membership／音素倒排放閘前

**背景預載標示**：
**就緒閘解鎖**後、**啟動完畢**前，向創作者傳達 tail **離線啟動預載**仍在進行；不阻**查詢分派**。Portable 進度須**誠實**反映 **詞庫快取索引** 與其他 tail 階段（唔應大段跳空）。PWA 涵蓋靜態詞林、**詞庫字面集**、**音素倒排**。
_Avoid_: warmup badge（作領域正名）、暖啟動（易與**詞庫暖啟動**混淆）、把標示當就緒閘 overlay

**詞庫字面集**：
詞條庫 DISTINCT 字面 membership，供近反義池衍生反義等「是否在庫」判斷；**就緒閘解鎖**後背景建表，進程內快取。
_Avoid_: 當閘前必備、與**詞庫快取索引**混稱

**音素倒排**：
PWA runtime 韻母／聲母倒排索引，供缺字錨等熱路徑縮候選；**就緒閘解鎖**後背景建表，首次查詢可 lazy rebuild。
_Avoid_: 閘前阻塞建表、當**韻母字母表**本身

**離線啟動預載**：分 **閘前**（阻塞**就緒閘解鎖**）與 **tail**（**就緒閘解鎖**後背景完成至**啟動完畢**）。Portable 閘前主為進程就緒 + **詞條庫可查探針**；**詞庫快取索引** 屬 tail。PWA 閘前主為詞庫開庫、**韻母字母表**／**排序信號**／**複合詞表**輔助索引與離線就緒驗證；兩渠道 tail 內容不同（見 **啟動完畢**）。

**創作者**：用本工具搜韻換字填詞者。

**全量發佈**：同 semver 整包（Portable zip／tar；新 tag 首次上傳時一併帶 **發佈詞庫快照** `lyrics.db`／`words-lexicon.json`）。**何時 bump semver（分級）**：**必須新 tag**——schema／查詢語法行為變更、大規模刪收錄、破壞離線快取假設、刻意產品大改（下一個產品 bump 例：`v1.0.9` → `v1.1.0`）。**可同一 tag 換庫快照**（refresh＋`-WithLexicon` 覆寫 Release 庫資產）——標音／來源合併修正、少量增刪讀音或字面、閘／煙霧對齊、發佈後短窗熱修；**須同時重打並上傳對應 Portable 套件**，令 Release 上庫資產與套件內庫／**套件發佈指紋**一致，禁止只換庫資產而套件仍舊。**程式-only 刷新**——庫不變時重打套件，**唔** `build-db`、預設**唔**覆寫庫資產。
_Avoid_：細換庫硬逼 bump、程式-only 刷新誤覆寫庫、刪掉 Release 上嘅 `lyrics.db`、把庫 commit 入 git 當預設、同 tag 只傳新庫唔重打包却期望 **套件更新提示** 引導下整包仍正確

**分渠道發佈**：發佈主理（建 tag、Win zip；新 tag 先上傳庫）與發佈補件（mac tar 對齊 tag）；補件不建 release。

**詞庫發佈**（已退役）：舊指只換同 semver Release 上 db／json、唔換程式嘅獨立層；已取消獨立 workflow。細換庫走 **全量發佈** 分級裡嘅「同 tag 換庫快照」；大換庫走新 tag。
_Avoid_：再開 `release-lexicon` 類 workflow、把 **詞庫發佈閘**（量度）當成發佈層

**發佈詞庫快照**：某 release tag 上供 Pages／補件對齊嘅 `lyrics.db`（及可選 `words-lexicon.json`）；由該 tag **首次全量發佈**寫入。程式-only 刷新預設保留唔覆寫；屬**可同一 tag 換庫快照**時可 `-WithLexicon` 覆寫。
_Avoid_：假設 tag commit 內必有 db、刷新時刪庫資產

**煙霧測試套件**：PR 必過；fixture 核心旅程 + 就緒 + 近反義不變量。

**黃金查詢集**：21 journey + 16 match_spec；parity gate 保 PWA=portable。

**查詢分頁**：固定 header 最頂、管理整個產品頁面嘅多上下文容器；沿用 header 既有固定行為，唔另造第二層 sticky chrome。同一常駐列可承載多個搜尋分頁，以及一個標題為「句格工作台」（英文 `VerseCraft Workbench`）、保留句稿狀態嘅**句格工作台**分頁，切換時唔做整頁重新載入；窄屏橫向捲動並保持作用中分頁可見。作用中分頁完整接管列以下嘅畫面：搜尋分頁先顯示搜尋 chrome／結果，工作台分頁先顯示工作台 chrome／句稿，唔互相嵌入。+ 永遠只開新搜尋；現有「句格工作台」入口建立或選中唯一工作台分頁；x 關閉、拖曳重排。關閉工作台分頁只收起工作面並保留句稿，真正刪稿只經**清空句格**。每個分頁各自保存離開前嘅垂直捲動位置、選中項目與詞條詳情／候選比較面板開關；新分頁由頂開始，面板只喺用戶主動關閉、項目失效或分頁關閉時清除。每搜尋分頁獨立回溯，模式／就緒全域。`/workbench/` 係可直接開啟嘅工作台入口：建立或選中唯一工作台分頁並恢復句稿；切換作用中分頁時 URL 同步反映該工作面但唔新增瀏覽器 history，Back／Forward 留畀搜尋分頁內查詢回溯；搜尋 URL 契約不變。切走分頁會取消未完成嘅搜尋／工作台候選運算；已完成結果與句稿保留，返回時按原狀態重查未完成部分。工作台候選「在搜尋頁查看」先以候選字面比對搜尋分頁嘅已提交查詢文字；字面完全一致就選中最左邊嘅現有分頁（模式／子模式／詞性篩選不參與），無先開新搜尋分頁並執行查詢。從**搜尋教學**／關於頂欄**送出**查詢（Enter／窄屏「搜尋」）時開新搜尋分頁並觸發**查詢分派**，保留教學／關於分頁（與教學可執行範例一致）；喺該兩類分頁打字只更新輸入與**查詢語意解釋**，唔因 debounce 切查詢分頁或派查。
_Avoid_：把**句格工作台**維持成繞過查詢分頁嘅獨立頁、切換或關閉分頁時重建／清空句稿、把每個搜尋結果誤稱為一個分頁

**模式切換**：**搜尋模式家族**（0243搜尋／**平仄**／近反義）頂層即切（寬屏頂欄；窄屏選單）；**0243搜尋模式**／**平仄** 旗下檔（鬆／緊／六聲）貼搜尋列切換，唔佔家族位。

**效能優化門檻**（維護者）：雙路證據驅動；創作者（就緒/查詢）+ build ≤10min；梯：SQLite→快照→純py→新依賴（最後）。

**開發容器環境**：維護者用；非創作者交付。

**替換條件面板**：句格工作台中，集中呈現本次候選替換所用條件的面板；由可收起的**條件內容**與保持可用的**操作列**組成。條件內容收起時不改變已選條件，只暫時隱藏控制項；重新進入或重新載入工作台預設收起，創作者主動展開後才查看／修改。

**替換條件操作列**：替換條件面板頂部的常駐操作區，包含面板展開／收起入口，以及 POS 篩選、復原等工作台操作。收起條件內容後，操作列仍然可見；**唔**把操作列誤當成條件內容的一部分。
_Avoid_：把面板收起當成清除或重設條件、把 POS 篩選或復原操作一併藏起、把跨頁保存的 UI 偏好當成替換條件資料。
### Canonical MatchSpec compiler (ADR-0076)

The grammar parses raw input into `ParsedQuery`; the canonical compiler owns
the semantic `ParsedQuery -> CanonicalMatchSpec` seam for every manifest
`match_spec` kind. `CanonicalMatchSpec` is immutable and contains width, ordered
slots, derived mask, equals span, compound/ranking/candidate-scope policy,
code mode, and explicit Jyutping branches. Query execution must not reparse
raw text or rebuild a spec. `compileReplacementPlan` is the separate
workbench input adapter and uses the same finalizer. The old registry and
grammar builders are migration adapters only; see ADR-0076.
