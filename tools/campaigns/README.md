# 戰役工具 (`tools/campaigns`)

維護者 one-shot（CONTEXT § **戰役工具**）。**唔**屬 **詞條庫建置命令**。

- 清單 collect／`ingest.project_pos`（含 `build` carrier）仍在 `ingest/`。
- 舊 `python -m ingest.project_pos_*` 經 shim 轉嚟呢度（DeprecationWarning）。
- 新入口：`python -m tools.campaigns.<module>`。
