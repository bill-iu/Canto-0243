# Maintainer / ingest only — NOT installed into Desktop wheel (ADR-0068).
#
# Product runtime:
#   pip install -r requirements.txt
#
# Full maintainer env:
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt
#
# CPU-only torch (optional, after requirements-dev):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# --- lexicon build / repair (not query-path) ---
pycantonese            # ingest + scripts/patch_phoneme_length_mismatches
pyjyutping             # ingest fallback when pycantonese misses
setuptools             # pyjyutping uses pkg_resources
pyyaml                 # sources / syn-ant manifests (ingest)

# --- already in requirements.txt; listed for standalone dev installs ---
opencc-python-reimplemented

# --- ML / relationships (heavy) ---
sentence-transformers
# torch pulled by sentence-transformers
numpy
cilin

# --- packaging (build machine only) ---
pyinstaller  # legacy portable launcher only (build-portable-legacy.ps1)
